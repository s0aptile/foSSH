# DURUM — status

## 0.0.2.2 — current

Last updated: 2026-08-16.

### Section review pass — what it found

Chiefs reviewed ingest, watchdog, surface, and the public-facing text.
Nine defects, every one of them silent: nothing about the running
system looked wrong in any of these cases.

| Severity | Defect | State |
|---|---|---|
| Privacy | Hidden groups recoverable by subtracting published groups from the ungrouped total — one subtraction returned a single visitor's exact hits and uniques | Fixed, complementary suppression. Residual now 0, measured. |
| Privacy | `X-Forwarded-For` read leftmost — the entry the client writes. Fabricated addresses inflate uniques past `k` and publish suppressed rows | Fixed, hop count from the right. 6 fake uniques → 2 real, measured. |
| Privacy | `k_anonymity = 0` or `1` disabled the fold entirely, valid from file or env | Fixed, floor of 2, refused at load. Second guard in `fossh-agent` said `>= 1` and is now shared. |
| Credential | PHP binding sent the write key as a Bearer token over `http://`, and neither transport disabled redirects | Fixed. Verified against a two-hop server: old code leaks, new does not. |
| Data loss | PHP binding's `FOSSH_KEY` never reached the FFI context — the documented configuration recorded nothing, permanently, silently | Fixed. |
| Availability | `fossh_record_env` dereferenced null envp entries, segfaulting the **host** process where `catch_unwind` cannot reach | Fixed. Test SIGSEGVs the runner without the guard. |
| Availability | Client-supplied `i64::MIN` timestamp overflowed pre-auth; panics under `overflow-checks`, and `panic="abort"` makes that fatal for every site on an fcgi process | Fixed, saturating. |
| Availability | PHP CGI fallback waited on a subprocess with no ceiling; killing it killed only the intervening shell | Fixed, 2s bound, no shell. |
| Usability | `fossh site disable` had no inverse — the only way back was editing SQLite | Fixed, `fossh site enable`. |

Two more are **mitigated, not fixed**, and are recorded as such rather
than counted above:

- **Watchdog check-then-exec.** The window between verifying a binary's
  hash and running it is down from the whole verification to two
  syscalls. Closing it needs `setpriv` to stop exec'ing by path. ADR-0075.
- **Differencing across many queries.** Subtraction against the total is
  closed; a caller solving many overlapping queries as a system is not.
  Structural to k-anonymity. Now in THREAT_MODEL.md's not-defended list.

### Corrections to this file's own kind of claim

`THREAT_MODEL.md` asserted that a narrow query "can't isolate an
individual by intersecting small groups." It could, and the
demonstration is three commands. The document now says what was wrong
and when, rather than being quietly edited.

`README.md` told a stranger to run `sudo dnf install fossh-console` for
a package in no repository, with no caveat. `docs/PACKAGING-copr.md`
recorded a link failure blocking every chroot that had already been
fixed and never retested — it is retested now, and all four packages
build.

### Verification

| | |
|---|---|
| Rust workspace | 641 tests |
| `fossh-ffi` (own workspace) | 33 tests |
| OCaml watchdog | 250 checks |
| PHP binding | 24 assertions (new suite, plain PHP, no PHPUnit) |
| RPM | `fossh`, `fossh-watchdog`, `fossh-console`, `fossh-selfheal` + SRPM, all at `1:0.0.2.2` |
| clippy / rustfmt | clean |

Every privacy and credential fix above was reproduced against compiled
binaries before and after, not argued from the source.

### Not done

- No Copr build attempted since the link fix. Fedora passing is an
  expectation, not a result.
- EPEL 9 still blocked by Copr's stale Rust (MSRV), untouched by this pass.
- `python3-pytest` absent, so the console's own suites cannot run here.
- `php-ffi` absent, so the PHP binding's FFI mode is reviewed, not run.
- `ocamlformat` absent, so `dune build @fmt` cannot run.

### Since the section review pass, same day: the advisory layer, fixed and wired in

Not covered by the table above — this work landed after it was written.
Two real defects in the two-model advisory layer, then the layer
actually connected to the console for the first time.

| Defect | State |
|---|---|
| Biased One (the text model): a `stop "</think>"` sequence halted generation before any answer token — 34/34 and 40/40 calls returned empty content across both models tested | Fixed, stop sequence removed, budgets raised to what reasoning actually costs. ADR-0076. |
| Biased One: no identity fence in the SYSTEM block, only a post-hoc term filter | Fixed, an explicit never-name-yourself instruction added; a demonstrated-refusal worked example was tried and reverted for breaking ordinary answers. ADR-0076. |
| Hellen's Eye (the vision model, `qwen3-vl:2b`): an adversarial image could talk it into self-disclosure, and several others locked it into a non-converging reasoning loop | Fixed by switching the underlying model to `qwen3.5:4b` with `think=False` — reasoning never starts, so it cannot loop in one. ADR-0078. |

`advisor_client.explain()` had existed since earlier in this session and
was never called from anywhere in the GUI — there was no findings-list
view for it to feed. `fossh-agent` gained a `selfheal.check` RPC method
(`crates/fossh-agent/src/main.rs`), and `overview.py` gained a real
Findings section (`Adw.ExpanderRow` per finding, advice filled in async
via a new `advisor_bridge.py` worker thread) that queues an explanation
for any finding above `info` severity automatically. `ConsoleApplication`
re-runs the deterministic checks every 900s while open
(`SELFHEAL_TICK_SECONDS`) — an in-console tick, not a headless daemon;
that remains deliberately unbuilt. Verified end to end against the real
running model, not a mock. ADR-0079.

`/run/fossh-selfheal` and a dedicated `fossh-selfheal` group are new:
the advisor's exclusivity lock needed a runtime directory a human
console user could actually reach, without widening `/run/fossh` itself
(the base package's own ingest runtime, unrelated to this). ADR-0080.
`packaging/selinux/fossh.fc` does not define a context for it yet —
flagged there as a real but currently inert gap, since nothing this
policy confines touches it today.

A three-agent doc sweep followed (`c8c93b1`) — version strings, a
retired-TUI dependency list still in `NOTICE`, and a `docs/SELF-HEALING.md`
claim about a scheduled tick that runs as `fossh-svc`, which does not
exist. `granite4.1:3b` was evaluated as a replacement for Biased One and
rejected, not shipped. ADR-0081.

## 0.0.2.1

Last updated: 2026-08-15. The chapter below this section describes the
0.1.x work and is kept as a record; where it and this section disagree,
this section is right.

### What changed

| Area | State |
|---|---|
| `fossh-tui` → `fossh-agent` | Done. Terminal layer deleted; every protocol client kept byte-for-byte. JSON-lines over stdio. 88 tests. ADR-0061. |
| `fossh-console` (GTK4/libadwaita) | Done. Four views, brand palette carried from `theme.rs`, WCAG 2.2 contrast asserted by test in both schemes. Rendered and inspected, not only compiled. |
| Integrations (API keys) | Done. Sealed at rest, redacted everywhere, `curl --config -` so the credential never reaches argv or environ. Verified against a live `/proc` inspection. ADR-0062, ADR-0063. |
| Providers | Done. Six bundled, declarative rather than loadable code. ADR-0066. |
| Self-healing | Done. Deterministic engine plus a fenced, capability-gated local model. 58 tests. ADR-0065. |
| RPM packaging | Done. Four packages build clean: `fossh`, `fossh-watchdog`, `fossh-console` (noarch), `fossh-selfheal` (noarch). |
| 0.1.x retirement | Done. `RETIREMENT.md`, `docs/UPGRADING-from-0.1.md`, tag `v0.1.3_oa-retired`. |

### Corrections to what this file previously claimed

Three rows of the table below asserted that tests were passing which
had, on the evidence, never passed. They are recorded here rather than
edited away, because a status file that quietly fixes its own history
is worth less than one that shows where it was wrong.

1. **§3.3/§3.9 — "real cross-language interop test against the compiled
   `fossh-watchdog`".** Both `fossh-agent` (then `fossh-tui`) interop
   tests were failing. The watchdog dropped privileges unconditionally
   via `setpriv`, which needs root; run as anyone else every spawn died
   at exit 127, the storm guard tripped in about a second, and the
   whole watchdog exited. Both tests sent its stderr to `/dev/null`, so
   the explanation was discarded. Fixed, and the tests now surface that
   stderr on failure. ADR-0064.

2. **§2.4 — `bootstrap_interop.rs` "cross-language-verified".** Failing
   separately, for never setting `LD_LIBRARY_PATH`: the binary died in
   the dynamic linker before `main` ran. Fixed.

3. **§3.10 — the reproducibility and hygiene claims.** These held, but
   `dist/` was never cleaned between builds, so a subpackage that
   changed architecture left its stale predecessor behind for
   `build-release-zip.sh` to glob. Fixed in the script rather than by
   remembering to `rm` first.

### Verified for 0.0.2.1, by running it

- Full Rust workspace: 0 failures.
- OCaml watchdog suite: clean.
- `cargo fmt` clean; `cargo clippy` clean across every crate touched
  (three pre-existing warnings remain in `fossh-core`/`fossh-ingest`,
  untouched here and out of this pass's scope).
- `cargo audit`: no advisories. `cargo deny`: advisories, bans,
  licences and sources all ok.
- `scripts/check-identity-hygiene.sh`: all checks passed.
- Ingest-path isolation: `cargo tree -p fossh-cgi`, `-p fossh-fcgi` and
  `-p fossh-ffi` contain neither `fossh-agent` nor `fossh-selfheal`.
- Agent protocol against 17 hostile inputs (malformed JSON, wrong
  types, 200-deep nesting, NUL bytes, path traversal, `file://`, CRLF):
  no panic, nothing accepted, every reply a single valid frame.
- Credential containment: with a real request in flight, the key
  appears in neither `/proc/<curl>/cmdline` nor `/proc/<curl>/environ`,
  nor stdout, stderr, or the sealed file. No temp files left behind.
- Four RPMs build and their contents were listed and checked.
- The console was rendered to PNG on every page and looked at.

### Open, and honest about it

- **The local model has never been run.** Ollama is not packaged for
  Fedora and is not installed here, so `advisor.rs`'s gating, prompt
  construction and fence are unit-tested but the end-to-end path —
  build the derived model, probe, generate, attach — has not executed
  once. Marked "written, unverified" in the sense this file has always
  used, and it is the largest unverified surface in 0.0.2.1.
- `keylock::verify` **is** now verified end to end against a real gpg,
  including the forged-status attack described in ADR-0067 — this was
  listed here as unverified and no longer is.
- **The Apache configuration has not been loaded by a real httpd.** The
  `Require expr ... file()` comparison is reasoned about carefully (see
  ADR-0065 on the two properties that make or break it) but not
  observed working.
- No third-party security audit.
- `%post` group membership (`usermod -a -G apache fossh-svc`) has not
  been exercised by a real install; it needs root.

---


Chapter-tracking file mandated by the "Fedora Native Deployment, Watchdog & Local Admin Hardening" chapter, created before any implementation work in that chapter, per its own instructions. Updated after every sub-chapter's QA gate, not just at the end. This file describes what is actually true right now; if something below turns out to be wrong, fix the code or fix this file, not the reader's expectations.

Last updated: 2026-08-06, later still (final release-artifact rebuild + a genuine CI/tooling closeout pass, see DECISIONS.md's ADR-0060): the RPM/SRPM rebuilt fresh with `wizard.rs`'s rewiring baked in (previous `dist/` build predated it), the public `fossh_github/` zip rebuilt on top of that and re-verified (zero identity-hygiene hits across every shipped binary, RPM/SRPM inside the zip confirmed byte-identical to the freshly built ones — the exact "stale artifact bundled into a 'fixed' zip" bug class ADR-0058 Finding 1 already caught once). §3.7's own "not yet done: wiring a short run into CI" is now done — a `fuzz-smoke` job added to `.github/workflows/ci.yml` running all 5 targets for 15s each on every push/PR. Found and fixed while validating that addition, not by design: `.github/workflows/ci.yml` had a genuine, previously-undetected YAML syntax error (an unquoted `: ` inside a `name:` value, ambiguous with a nested mapping under the YAML spec) that predates this session entirely — confirmed against the last *committed* version of the file, not just the working copy — meaning this repository's CI has likely never once successfully parsed and run, on any push or PR, since it was added. Fixed by quoting the value; the whole file now parses clean. Also closed the identity-hygiene script's own documented structural gap (this file's previous entry, below, noted it only scanned `git ls-files`-tracked files and suggested `git add -A` as a stopgap before each run) — the actual fix applied instead: `check-identity-hygiene.sh` now enumerates `git ls-files --cached --others --exclude-standard` (tracked + untracked-but-not-gitignored), closing the gap at the scan itself rather than requiring a remembered manual staging step before every future run; re-verified both directions (`test-identity-hygiene-gate.sh` still fails its deliberately-bad fixture, and the real check now genuinely covers this session's ~68 untracked files too, not just the previously-scanned tracked ones — clean).

Previous entry, same day (`wizard.rs` rewritten to speak the real §2.6/§3.11 SETUP protocol — closes the gap this file's own top entry flagged open just below, see DECISIONS.md's ADR-0059 — on top of the real clean-VM install verification run for the first time ever, plus a full round of fixes it and a comprehensive QA pass surfaced, the §2.1 auth gate's Rust-side client, and TUI live-query wiring). In order:

**Clean-VM install verification (prrr.md §5's own definition-of-done item, never run before this).** Rootless `qemu:///session` KVM VM from the existing Fedora 44 Live ISO. Found and fixed 3 real bugs a dev-machine-only `rpmbuild --nodeps` build structurally could never catch: (1) `scripts/build-release-rpm.sh`'s `git stash create` silently drops untracked new files from the source tarball — fixed with a scratch `GIT_INDEX_FILE` snapshot (`read-tree HEAD` + `add -A` + `write-tree` + `commit-tree`, still never touching the real index/HEAD/stash list); (2) the RPM's `%pre`-created `fossh-svc`/`fossh-watchdog` users/groups had no matching `Provides: user()/group()`, so a real `dnf install` failed dependency resolution outright (`rpmbuild --nodeps` never exercises this) — fixed with 4 added `Provides:` lines; (3) `packaging/selinux/fossh.fc`'s three file-context lines used the unexpanded `gen_context(...)` M4 macro (this module deliberately avoids the M4 interface library per its own header comment, but the .fc file still had a literal macro call in it) — `checkmodule`/`semodule_package` never validate `.fc` syntax, only bundle it, so this compiled and packaged "clean" every time and only broke on a real `semodule -i`, exactly what this verification ran for the first time — fixed by hand-expanding to literal `user:role:type:level` contexts (confirmed against this machine's own real `/etc/selinux/targeted/contexts/files/file_contexts`). Beyond the fixes: real `dnf install` succeeded end to end post-fix; real `%pre` user/group creation confirmed; `fossh-watchdog.service` correctly **refused to launch** with no manifest present (§3.6's fail-closed design working exactly as documented, not just claimed); real AVC-clean service starts under `setenforce 1`; and the full §2.6/§3.11 setup-token wizard flow (`fossh-tui` run interactively over serial console) completed end to end including confirmed reuse-prevention after burn. **Important scope note, found by a later live end-to-end test the same day:** this VM run happened *before* the real, watchdog-owned `Setup_token`/`Operator_auth_server` SETUP flow existed — `crates/fossh-tui/src/wizard.rs` (the actual "Setup Wizard" screen, key `3`) is still its own self-contained standalone/demo-mode implementation exactly as its own module doc has always said, generating and verifying its own token entirely in-process. It was never updated to speak the real wire protocol once that protocol existed. Only the separate `OperatorAuth` screen (key `4`/`a`) speaks the real `Operator_auth_server` protocol, and only its challenge-response half, not the SETUP half. A real live test of the full real flow (setup-token submission, enrollment, challenge-response, replay/reuse protection) was run successfully the same day, but by hand-rolling a client against the real protocol, not by driving `fossh-tui`'s own wizard screen. **This gap is now closed** (DECISIONS.md's ADR-0059, same day) — `wizard.rs` was rewritten to read the real token file, drive `operator_auth_client`'s new `setup`/`setup_at` against the real socket, paste-or-generate a real key, and reflect the real `SETUP_OK`/`SETUP_DENIED`/`ENROLLED`/`ENROLL_FAILED` replies, with a real cross-language interop test against the compiled watchdog binary and a dedicated adversarial-review pass (one HIGH finding, a client-side drip-attack timeout gap mirroring one the server side had already fixed once — closed). The clean-VM run above still predates this fix and was never re-run against it; that remains an open follow-up, not silently claimed covered.

**Watchdog systemd unit** (`packaging/systemd/fossh-watchdog.service`, new — closes the gap §3.3/§3.11 both flagged as open). Execs `fossh-fcgi` directly (supervised, not via `fossh-fcgi.service`); `JoinsNamespaceOf=fossh-fcgi.service` is load-bearing, not decorative — both units set `PrivateNetwork=yes`, and without sharing the namespace the §3.4 QUIC channel's default `127.0.0.1:7443` in one unit's private loopback would not be the same loopback as the other's, silently breaking the channel (fails soft/logged on both sides, so this would never have surfaced as a startup error). Also closed a real, separately-tracked gap the same pass: `/var/lib/fossh-watchdog` was never actually created/owned by the package at all (`useradd`'s own `-d` referenced it as a homedir without `-m`) — added `install -d`/`%attr`/`chown` for it, matching `/var/lib/fossh`'s existing treatment.

**RPM release build now actually includes the §3.4 QUIC channel.** `scripts/build-release.sh` (which is what the RPM spec's own `%build` calls, not a separate cargo invocation) now builds with `--features fossh-fcgi/quic,fossh-tui/quic` — previously the reload/status channel this chapter spent most of its effort on was never compiled into the shipped binaries at all, dev-default builds correctly exclude it (ADR-0050 Part 3's own reasoning, heavy BoringSSL compile), but nothing had made the *release* build the exception. This surfaced two more real, previously-invisible gaps, both fixed: (a) root `deny.toml` had no `ISC` in its license allow list (unlike `fossh-ipc`/`fossh-quiche-ffi`'s own, which already did) — `cargo deny --features fossh-fcgi/quic,fossh-tui/quic check` genuinely failed on `libloading`'s ISC license (pulled in by `bindgen` for BoringSSL's own bindgen step) until fixed; (b) `boring-sys`'s own independent BoringSSL vendoring (separate from `crates/fossh-quiche-ffi`'s already-remap-fixed one) leaked the real build machine's username/home path into `fossh-tui` via C++ debug-info strings — the exact same leak class ADR-0054 already fixed once for a *different* BoringSSL build path, missed here because this path was never exercised by an ordinary release build until this session started passing `--features quic` to it. Fixed with `CFLAGS`/`CXXFLAGS -ffile-prefix-map` in `scripts/build-release.sh`, verified clean (zero `strings` hits for the real username/hostname) across all 6 shipped binaries after a forced clean rebuild — the first attempt at verifying this fix gave a false "clean" result because `cargo clean -p boring-sys` silently removed 0 files and the stale cached build was reused; only a direct `rm -rf` of `target/*/build/boring-sys-*` actually forced the real recompile needed to prove the fix.

**§2.1's TUI-side client (Rust) and live-query wiring, both new.** `crates/fossh-tui/src/operator_auth_client.rs` + `operator_auth.rs`: connects to `Operator_auth_server`'s Unix socket per its own `.mli` wire-protocol spec exactly (byte-for-byte discipline on the nonce framing — the `str::trim()`/`String.trim` divergence class ADR-0050 already found once is exactly what this avoids), shells to `gpg --armor --detach-sign` using the operator's own local GPG identity (the first place this project's Rust side has ever shelled out to `gpg` — OCaml does throughout `watchdog/lib/`), real cross-language interop test spawning the actual compiled `fossh-watchdog` binary. Separately, `crates/fossh-tui/src/watchdog_status.rs` (new) gives the dashboard its first real watchdog/tamper-detection live query over the existing §3.4 QUIC channel — required extending `command_protocol.ml`/`command_client.rs` with a new `Status` command (both sides' `decode_response` parsers extended in lockstep, again matching the trim-divergence lesson) and having the watchdog *re-run its real tamper-check gate on demand* rather than reporting a cached belief. Dashboard's stale "not built yet (task #37)" placeholder is gone; both screens render honest failure states, never fake data, when the watchdog isn't reachable. Full workspace (`cargo build/clippy/test --workspace`, both default and `--features fossh-fcgi/quic,fossh-tui/quic`) clean; `fossh-tui`'s own test count now 32 (up from 21).

**Comprehensive pass** (fuzz long-run, supply-chain re-audit, identity-hygiene re-sweep, two independent QA passes on the public release output) — all run this session, not deferred: real long (~76 min, not 15s) `cargo-fuzz` run across all 5 existing targets, **933,900,543 total executions, zero crashes** (`json_body` 49.2M, `query_string` 29.0M, `forwarded_for` 494.6M, `fcgi_framing` 201.0M, `spool_frame` 160.1M) — `THREAT_MODEL.md`'s own "short smoke tests only" alpha caveat was stale at the time this entry was first written; **fixed later the same day** alongside the CI `fuzz-smoke` job addition (see this file's top entry) — it now states the real long-run numbers and that a smoke run is CI-wired. `cargo audit`/`cargo deny` re-clean across all 4 Rust workspaces post the ISC fix above; opam inventory unchanged (9 packages, `Setup_token` pulled in nothing new); `check-identity-hygiene.sh` clean, with one real structural gap noted at the time (it only scanned `git ls-files`-tracked files, so this session's many new untracked files were never automatically covered — verified clean by hand instead). **Closed later the same day, properly**: see this file's top entry — the script now scans tracked + untracked-but-not-gitignored files directly, no manual staging step needed. Public output (`fossh_github/`) QA found and fixed: `documents.zip` had gone missing entirely (rebuilt); a stale Apache-2.0 §6 trademark citation in `NOTICE`, the same bug class ADR-0038 already fixed once in `tos.md`, missed here (fixed in both the source repo's and the public copy's `NOTICE`); the public `README.md`'s Fedora-native line had silently dropped the "(in progress)" qualifier the source repo's own `README.md` correctly keeps, overclaiming `dnf install fossh` as already-working (fixed); both `README.md`s gained a "What it will never do" section (previously only in the release-zip's shorter `readme.md`, the single strongest piece of concrete privacy-invariant content either doc has, missing from the primary GitHub-facing page). A real, local (no ngrok, no public exposure) stress test against `fossh-fcgi` — 2000 concurrent requests, ~2750 req/s — confirmed no crash, correct rate-limiting behavior under load (429s, not silent drops or corruption), and correct data landing (666 hits/663 uniques, matching exactly what was actually accepted).

Previous entry, same day, §3.3's last remaining open item closed: `Operator_key.enroll` is now gated by real proof of the §2.6 setup token, not reachable unconditionally any more — new watchdog-owned `Setup_token` module, extended `Operator_auth_server` wire protocol, wired into `main.ml`. Adversarial review — a dedicated subagent plus real bugs found by this task's own required concurrency testing — caught 2 CRITICAL (an uncaught-`Sys_error` crash path on every restart under a real disk-full/read-only-tmp trigger; an O(n²) buffer-rescan CPU-burn DoS from a single ordinary write at the new, larger public-key read cap), 1 MEDIUM (a pipelining client's key-block bytes silently discarded), and — independently, while building the mandated real concurrency test, not by the subagent — a genuine, previously-unnoticed bug in `Operator_key.ml` itself (already reviewed and believed settled): two temp-path constructions named from `Unix.getpid()` alone collide across concurrent *threads* of one process (not just processes), reproduced with a real countdown-latch-forced 6-thread race, all fixed. Full watchdog suite (16 binaries, 221 checks) 3 consecutive clean serial runs. See ADR-0057. Previous entry, 2026-08-04: full workspace/`fossh-ffi`/watchdog test suites + clippy re-verified clean, then a dedicated doc-staleness sweep found and fixed 8 more stale "not built yet" doc comments — same class as the `ui.rs`/`DECISIONS.md` bugs already caught — plus a stale Ruby binding version and two stale `Cargo.lock`s in workspace-excluded crates; zero new identity leaks, confirming ADR-0054/0055 held — see ADR-0056. Earlier same day, 0.1.2_oa refinement pass, still release 1: `fossh-tui`'s dark-graphite theme completed (explicit background/foreground fill, ~11:1 contrast, release 1 stays dark-mode-only), stale dashboard/§3.3-row status text corrected (watchdog and tamper detection are built and verified — the TUI just has no live query wired to either yet), and two more real identity leaks found and fixed: the new `scripts/build-release-rpm.sh`'s own `_topdir` override still resolved under this checkout's real path (caught by the script's own self-check, not shipped), and `DECISIONS.md`'s own ADR-0054 entry had been illustrating its findings by reproducing the actual leaked username/hostname/path — see ADR-0055. Earlier same day: task #26 prep found and fixed a real identity leak in the shipped RPMs and vendored `libquiche.so` — real username/hostname baked in via `rpmbuild` defaults and BoringSSL's own C/C++ paths, neither caught by the existing hygiene tooling; both fixed, plus a real gap in the hygiene script's own regex — see ADR-0054. Also fixed that pass: real passphrase protection for the watchdog's OpenPGP key, previously empty — ADR-0053. Correction to the entry below: "every sub-chapter done" was premature — §3.3's own §2.1 auth-gate responsibility is genuinely still open, deliberately not built this session — see ADR-0052, not silently different from what ADR-0051 claimed. Earlier still: §3.5/§3.6 done via real filesystem-permission tests against the real `fossh-svc`/`fossh-watchdog` system users — ADR-0051. Previous entry, 2026-08-03: §3.4/task #31 done, real QUIC wiring adversarially reviewed — 2 CRITICAL + 1 HIGH + 2 LOW real bugs found and fixed; see ADR-0050).

**Running the watchdog's own test suite directly** (`cd watchdog && eval $(opam env) && dune test --force`, outside of `scripts/build-release-rpm.sh`'s own %build/%check, which handles this internally): every test binary under `watchdog/quic/` links dynamically against the vendored `libquiche.so.0` (§3.4), which isn't on the default dynamic linker search path — set `LD_LIBRARY_PATH="$PWD/quic/vendor:$LD_LIBRARY_PATH"` first (from within `watchdog/`), or every one of those binaries fails with `error while loading shared libraries: libquiche.so.0: cannot open shared object file`, not a real test failure. `scripts/build-quiche-ffi.sh` (also `cargo clean`-sensitive: cleaning `crates/fossh-quiche-ffi`'s own `target/` removes this vendored `.so` and it needs rebuilding before the watchdog suite can run again) is what produces it in the first place.

## Baseline (prior session, not re-implemented here)

The core product — CGI + embedded-FFI privacy-preserving telemetry — was built end to end before this chapter started:

| Milestone | Crate(s) | State |
|---|---|---|
| M1 | `fossh-core` | Done. Pure logic: validation, path/UA sanitization, HLL, histograms, config. 89 tests. |
| M2 | `fossh-store` | Done. SQLite storage, interning, rollups, retention, S8 file-permission enforcement. 42 tests. |
| M3 | `fossh-ingest`, `fossh-cgi` | Done. Auth, rate limiting, salt rotation, spool, CGI handler. 70 + 13 tests. |
| M4 | `fossh-cli` | Done. `init`/`site`/`query`/`maintain`/`export`/`doctor`. 20 tests. |
| M5 | `fossh-ffi` | Done. C ABI, `cbindgen` header, Miri-clean on the two pointer-touching functions, 32 tests in its own workspace. |
| M6 | `bindings/{go,php,ruby}` | Done. Go, PHP, Ruby — each with a runnable example and an integration test. None run against a real toolchain (none installed here); see below. |

267 Rust tests passing across the two workspaces as of the last full run (superseded — see "This chapter" below for the current count), zero clippy warnings, zero `rustfmt` diffs. Verified against real compiled binaries (CLI, a simulated CGI cycle, an independently-compiled C program linking `libfossh.so`), not just `cargo test`.

**M6 detail (finished mid-chapter, not part of the original pre-chapter baseline — this row and paragraph describe the *current*, completed state, not a snapshot frozen at chapter start):** Go binding (`bindings/go`) — cgo wrapper + `nofossh` no-op build tag + integration test + `examples/go`. PHP binding: `composer.json` and `src/Client.php`, three transports tried in order (FFI, HTTP-remote for real shared hosting — see `docs/INTEGRATION-php.md` — then CGI-subprocess, then a documented no-op), plus `bindings/php/src/Laravel/FosshMiddleware.php`, `bindings/php/src/Symfony/FosshRequestSubscriber.php`, and `examples/php`. Ruby binding (`bindings/ruby`) — pure-stdlib `Fiddle` wrapper (no native gem compilation), a Rack middleware, and `examples/ruby`. `docs/INTEGRATION-{php,go,ruby}.md` all written. Still true: none of the three run against a real Go/PHP/Ruby interpreter — none installed in this environment — reviewed by hand instead in every case.

**Repository relocated** to its current path at the start of this chapter (plain `mv` within the author's own home directory, git history intact, nothing recommitted or rewritten). The absolute path itself is not repeated here — see §19.4.12's identity-hygiene gate on real home-directory paths.

## This chapter — sub-chapter status

Status values: **not started**, **in progress**, **written, unverified** (code exists but couldn't be built/run here — toolchain gap, noted), **QA-gate passed** (implemented + adversarial subagent review completed + fixes applied), **blocked**.

| § | Sub-chapter | Status | Notes |
|---|---|---|---|
| 3.1 | glibc gatekeeping | QA-gate passed | Implemented, not yet load-bearing anywhere (see retrospective) — no systemd unit exists yet to call `glibc-check` automatically. |
| 3.2 | Native CGI hardening (privilege drop, systemd, SELinux) | QA-gate passed | Privilege-drop code and systemd units held up under adversarial review with only theoretical/cosmetic nits. The SELinux module had three concrete missing-grant bugs, all fixed and re-verified to compile/package; one architectural question (which domain `fcgiwrap` actually execs from) remains open pending a live, rooted load — see retrospective. |
| 3.3 | OCaml watchdog | QA-gate passed; logic layer, §2.4 bootstrap handoff, §3.4's QUIC wiring, and §2.1's setup-token-gated enrollment all built and adversarially reviewed. **No longer open**: `packaging/systemd/fossh-watchdog.service` (the dedicated watchdog systemd unit) and the Rust/TUI-side client that speaks `Operator_auth_server`'s wire protocol end to end (`operator_auth_client.rs`/`operator_auth.rs`) are both built — this row previously said otherwise after they'd already landed; corrected here to match this file's own top entry, which had the real state right. | No longer toolchain-blocked — `ocaml`/`opam`/`dune`/`gcc-c++`/`selinux-policy`/`rpmlint` all installed (see environment facts). `watchdog/` dune project: `Nonce` (CSPRNG via `/dev/urandom`), `Auth` (challenge-response verification via full `gpg --verify` + `--status-file`, pinned to an expected key fingerprint), `Session` (short-lived in-memory tokens), `Manifest` (clearsigned tamper-detection manifest, `sha256sum`-based, requires the manifest to explicitly cover the program being executed), `Supervisor` (spawn/waitpid `fossh-fcgi`, restart-storm guard, fail-closed tamper gate before *every* spawn including the very first one), `Gpg_status` (parses GnuPG's `--status-file` protocol for revocation/expiry, cross-checked against the authoritative `/usr/share/doc/gnupg2/DETAILS`). Adversarial review (ADR-0040 built it, ADR-0041 reviewed it) found 10 real findings — 2 critical (initial spawn bypassed the tamper check entirely; manifest content wasn't actually bound to the executed program), 3 high (`gpgv` doesn't check key revocation/expiry at all; a subprocess deadlock on manifest content past ~64KB; four uncaught-exception crash scenarios), 1 medium (no way to distinguish a tamper-refusal exit from a clean one), 4 low — all fixed and re-verified against the real compiled binary, not just re-read. 41 real tests across 5 binaries (up from 31), all passing, 3 consecutive clean runs post-fix, zero lingering `gpg-agent` processes. **§2.4 bootstrap handoff: both sides built, tested, and cross-language-verified** since the above was written. `fossh_admin::watchdog_pin` (Rust, core's/`fossh-svc`'s side): binds a Unix socket, accepts one connection, verifies the peer's real UID via `SO_PEERCRED` (`nix::sys::socket::sockopt::PeerCredentials`, safe, no `unsafe`) against an expected watchdog UID, persists the fingerprint via the same temp-file-then-hard-link pattern `data_key.rs` established, refusing (not silently overwriting) if a pin already exists — "regeneration requires a full re-bootstrap, not a silent rotation" enforced at the actual write, not just documented. `watchdog/lib/bootstrap.ml` (OCaml, watchdog's side): `send_fingerprint`/`send_fingerprint_with_retry`, exposed through a real `fossh-watchdog bootstrap-send <socket> <fingerprint>` CLI subcommand, not just a library function tests call. 22 Rust unit tests + 6 new OCaml tests (watchdog's own suite now 47 checks total, up from 41) plus one genuine cross-language integration test (`crates/fossh-admin/tests/bootstrap_interop.rs`) that spawns the real compiled OCaml binary against the real Rust listener over a real socket — catches a protocol mismatch neither side's own same-language tests could ever see. See ADR-0042. **The watchdog now generates and reuses its own keypair** (`Keypair.ensure_keypair`, idempotent — same fingerprint returned across restarts, confirmed both by return value and by directly counting secret keys in the homedir afterward), wired into `bin/main.ml`'s actual startup: the manifest is now always verified against the watchdog's *own* fingerprint rather than a separately-supplied CLI argument, which was a real footgun (an operator could pass the wrong fingerprint by mistake) removed by this change, not just a convenience. `test_helpers.ml`'s `generate_key` (used by nearly every existing test) now delegates to this same real function, so the whole existing test suite incidentally exercises it too. 52 real tests now (up from 47). Real, manual end-to-end smoke test against the compiled binary: generate-on-first-run → sign a real manifest with the generated key → run again → real spawn, real verify, real clean exit. See ADR-0043. **Still not built**: automatically calling `bootstrap-send` from this startup path — deliberately deferred, not an oversight; `main.ml`'s own header comment explains why (the handoff is one-shot, and this binary has no local record of "already handed off" independent of asking core, whose own listener may simply no longer be there after a prior success — needs deliberate design, not a rushed addition). Also still open: wiring either side of §2.4 into a real systemd service-startup path. **§2.1's remaining gap (ADR-0052) is now closed**: new `watchdog/lib/setup_token.ml`/`.mli` (watchdog-owned §2.6 setup-token generation, idempotent generate-or-recover from `/etc/fossh/setup-token`, hash held only in memory, burn ordering mirroring `setup_token.rs`'s own documented rationale) and an extended `Operator_auth_server` wire protocol (a first-run `SETUP <token>` → `SETUP_OK` → armored-key-block → `ENROLLED <fingerprint>` flow, reachable only immediately after `NOT_ENROLLED` and permanently unreachable again once a key is enrolled) that now actually gates `Operator_key.enroll` — confirmed by a full-repo grep that this is `Operator_key.enroll`'s only caller anywhere. Adversarial review (a dedicated subagent, plus this task's own mandated real concurrency testing) found and fixed: 2 CRITICAL (an uncaught `Sys_error` in the token-hashing path that could crash the whole watchdog on every restart under a disk-full/read-only-tmp condition — a fourth instance of a bug class already fixed three times elsewhere in this codebase; an O(n²) buffer-rescan in the socket read loop that turned into a real, measured 65+ second single-threaded CPU burn — starving every other connection — from one ordinary write at the new, larger public-key read cap), 1 MEDIUM (a pipelining client's already-buffered key-block bytes silently discarded, causing a spurious enrollment failure), and 1 LOW/MEDIUM (a multi-client test that claimed to exercise real concurrency but couldn't, given the accept loop's own single-threaded design — rescoped, and a genuine forced-overlap test added at the module level via a real countdown latch). That forced-overlap test then found a real, independent, previously-unnoticed bug in `Operator_key.ml` itself (already reviewed and believed settled): both `enroll`'s temp gnupghome path and `persist_fingerprint`'s temp file path were named from `Unix.getpid()` alone, which collides across concurrent *threads* of one process (not just processes) — reproduced with a real 6-thread latch-forced race (every thread failed with garbled `gpg` errors pre-fix, not one clean winner and five clean refusals), fixed by folding a fresh `Nonce.generate()` into both paths. Not reachable through this project's real, current callers today (the accept loop is single-threaded and fully serial), but the module's documented contract made no such caveat. A third, structurally identical instance (`core_pin.ml`'s `persist_pin`) was found by inspection and deliberately left unfixed — out of scope (§2.4's bootstrap handoff, a different, one-shot subcommand with no concurrent callers in this task's path), flagged rather than silently left. 50 new tests (`test_setup_token.ml`, `test_setup_enroll_flow.ml`, plus a direct regression test added to `test_operator_key.ml`); full watchdog suite now 16 binaries, 221 checks, 3 consecutive clean serial runs, plus the concurrency-sensitive binaries individually stress-run 8–15 times beyond that. See ADR-0057. |
| 3.4 | QUIC IPC (quiche via ctypes) | Task #31 done: transport layer, app-level command protocol, real X.509 identity generation, and now the real wiring into `fossh-fcgi`/`fossh-watchdog` (one command per connection, `SIGHUP`-triggered on core's side) all built, tested, adversarially reviewed (2 CRITICAL + 1 HIGH + 2 LOW found and fixed), and cross-language-verified | `cmake` and `clang-devel` (needed for BoringSSL's own bindgen step) both now installed by the user — no more toolchain blockers. `crates/fossh-quiche-ffi` + `scripts/build-quiche-ffi.sh` produce a real `libquiche.{so,a}` + `quiche.h` for the OCaml side's future `ctypes` bindings (quiche's own `ffi` module is a *private* Rust module — its symbols only get real ELF visibility when quiche itself is the crate compiled directly to a cdylib, not a dependency of a wrapper crate; confirmed empirically — a wrapper crate exported zero `quiche_*` symbols, quiche's own source built directly exported all 170 — so the script vendors quiche's exact pinned-by-Cargo.lock source into a gitignored build directory and builds it there). `crates/fossh-ipc` is core's actual Rust-side connection logic (safe API, no `ctypes`), with a real integration test driving a genuine mTLS handshake between two independently-generated, specifically-pinned certificates. Three real bugs found and fixed by actually running it, not just reading it back: a `done`-check ordering bug that silently dropped queued stream data when a caller only wanted "send this, then stop"; a fatal-`recv()`-error path that skipped the send-drain phase needed to actually notify the peer, then (in the very next fix attempt) silently swallowed that same error instead of propagating it; and a test that itself asserted the wrong property, expecting `is_established()` to flip to `false` immediately on a rejected certificate rather than allowing for TLS 1.3's real, documented asynchrony between "my own handshake looks done" and "the peer told me it isn't." 5 consecutive clean test runs. See ADR-0044. **A fourth issue, this time a performance bug, was found by a follow-up benchmark pass, not by testing correctness again**: `set_socket_timeouts`'s 100ms `UdpSocket` read timeout was silently costing a full 100ms on the last (empty) `recv_from` of every single `drive_until` pass, since a blocking socket with a timeout cannot distinguish "no more data, ever" from "check back very soon" without waiting the timeout out. Measured with a real 100-iteration benchmark: 211ms median handshake / 627ms median full round trip at 100ms, versus 8.8ms / 21.3ms at 2ms — roughly 24x/30x faster, same code, same certificates, only the timeout value changed. Fixed to 2ms, re-verified correct (3 more consecutive clean test runs, clippy clean). See ADR-0045. **The OCaml side (watchdog's) is now built too**: `watchdog/quic/` — `quic_shim.{h,c}` (a small C shim wrapping the handful of `quiche_*` functions that take raw `struct sockaddr*`, using real `inet_pton` rather than hand-rolled OCaml struct layouts), `quic_bindings.ml` (raw `ctypes`/`Foreign.foreign` declarations), and `quic.ml` (the safe wrapper — a deliberate close port of `fossh-ipc`'s already-debugged connection-driving logic, preserving both ADR-0044 phase-ordering fixes and ADR-0045's 2ms poll granularity rather than needing to rediscover either). Getting this to actually run surfaced three real, non-obvious linker/FFI-visibility problems (`ctypes`' `Foreign.foreign` is a runtime `dlsym`-equivalent, invisible to the static linker's notion of "is this referenced," requiring `--no-as-needed` + `--export-dynamic` for `libquiche.so` and a genuine compile-time-referenced OCaml `external` to stop the linker from dropping this project's own C shim object entirely; a `--whole-archive` attempt at static linking instead produced real duplicate-symbol errors from BoringSSL's own CPU-dispatch variants; a `.so`'s runtime dependency is resolved by SONAME, not its link-time filename) — all solved empirically, all documented in ADR-0046. Adversarial review found 9 real findings, most seriously a reproduced double-free (`Quic.close` was not idempotent) and an RPM packaging step-ordering bug that would have aborted every package build — all fixed, `Quic.close`'s fix pinned down with a dedicated regression test. `packaging/rpm/fossh.spec` updated in the same pass (new BuildRequires, a `%build` vendoring step, `libquiche.so.0` packaged alongside `fossh-watchdog` via a package-private lib dir + `ld.so.conf.d` + `ldconfig`). Full watchdog suite (8 binaries, 57 checks) clean, 3 consecutive runs. See ADR-0046. A full `rpmbuild -bb --nodeps` against a real `git archive` tarball of the committed tree (not a hand-picked file subset) was then run specifically to verify this packaging addition, not just the code: `%build`/`%check`/`%install` all completed, producing a real installable RPM; `rpm -qlp` confirmed `libquiche.so.0` and its supporting files land exactly where intended; `rpmlint` against the built package found nothing new and actionable beyond one already-explained, confirmed-unreachable finding (`binary-or-shlib-calls-gethostbyname` on `fossh-watchdog`, traced to OCaml's own standard `Unix` module, not to anything this codebase calls — full detail in ADR-0046). **The app-level command protocol is now built too**: `watchdog/lib/command_protocol.ml` (+ `.mli`) — restart/reload commands bound to a fresh, per-connection `Session` token, reusing §3.3's existing session module rather than inventing a new one, deliberately QUIC-independent pure logic (tested without a live connection, matching `Auth`/`Manifest`/`Session` themselves). Adversarial review found 5 real findings, all fixed — most seriously an API shape (`verify_command`'s `expected_token` and `presented_token` both plain `string`s) that let the natural-but-wrong integration silently collapse the whole check to a connection-agnostic "is this token valid anywhere," reintroducing exactly the replay this module exists to prevent; fixed by making the session token an abstract type via a new `.mli`, so that specific mistake is now a compile error. Also fixed: `Session`'s 15-minute default lifetime (sized for §2.1's original bounded-request use, not this channel's meant-to-persist-for-the-install's-uptime one) via a new `Session.touch`, called automatically on every successful verification; a token-field parser looser than the format it claims to enforce (accepted embedded newlines); unbounded echo of attacker-controlled text in error responses. This pass also closed a specifically-deferred item from ADR-0041: constant-time session-token comparison (`Nonce.constant_time_equal`), flagged there and left unfixed because nothing reachable at the time compared a token against attacker-influenced network input — `command_protocol.ml`'s `verify_command` is that first reachable path. See ADR-0047. Full watchdog suite now 9 binaries, 90 checks, 3 consecutive clean runs. **Real, tracked follow-up, not silently assumed covered**: a peer's stream-level `STREAM_STOPPED`/`STREAM_RESET` not always being caught by `drive_until`'s `is_closed()` check, on *both* sides of this channel (a pre-existing characteristic of the shared design, not new to the OCaml port). **Starting to wire either side into a real service (task #31) surfaced a real, previously-unnoticed gap**: nothing in this project generated an actual X.509 certificate for either the watchdog or core — the existing bootstrap handoff (ADR-0042) pins the watchdog's OpenPGP manifest-signing key, a completely different key/format from what `quiche`/BoringSSL's mTLS needs. Full plan documented in ADR-0048 (which side listens vs. connects — decided: watchdog `accept_one`s, core `connect`s, read from both sides' actual existing code shape, not assumed; the bootstrap handoff's wire format needs to grow to carry a full certificate PEM, not just a short fingerprint — not yet started; background-thread integration on both sides — not yet started; actual command dispatch — not yet started). **This pass built the watchdog-side piece**: `watchdog/lib/tls_identity.ml` — real, idempotent, 10-year-validity EC P-256 X.509 identity generation via `openssl req -x509`, mirroring `Keypair.ensure_keypair`'s shape. Adversarial review found 4 real findings, all fixed and regression-tested — most seriously a reproduced race (two concurrent callers on the same directory could produce a permanently mismatched, silently-"valid" cert/key pair in a real, measured 12.5% of trials), fixed with a `Mutex` and pinned down with a real 20-thread concurrency test; also an expired-certificate detection gap and a `-subj` Subject-DN injection footgun, both fixed. See ADR-0048. Full watchdog suite now 10 binaries, 110 checks, 3 consecutive clean runs. **Core's Rust-side equivalent is now built too**: `crates/fossh-admin::tls_identity`, deliberately more ambitious than the OCaml side's `Mutex`-only approach — genuine cross-process safety (many separate `fossh-svc` instances racing, not just threads), generalizing `data_key.rs`'s own already-shipped hard-link atomicity from one file to a matched pair. This took three real, sequentially-discovered concurrency bugs to get right — a directory-rename approach that always fails onto non-empty destinations regardless of whether the content was stale garbage or a legitimate winner; a 100%-reproducible follow-up bug in the symlink-based fix's own defensive handling; and, found by adversarial review after the first two were already stress-tested clean, a CRITICAL bug where the write side was atomic but two separate reads *through* the same mutable published pointer were not, reopening the exact mismatched-cert-and-key failure class the whole redesign was built to prevent, just via the read path instead of the write path. All three fixed and regression-tested (100+ consecutive clean runs of the specific concurrency test after the final fix). Also found and fixed in the same pass: every racing caller's generation directory being retained forever rather than just the winner's (accepted as a real but bounded, rare-event characteristic rather than adding GC logic this pass); a stray temp symlink leaked on certain failure paths; directories created at umask-dependent permissions instead of an explicit `0700`. A related, unrelated discovery: two pre-existing, already-shipped tests in `watchdog_pin.rs` (§2.4's bootstrap handoff) turned out to be timing-fragile under real parallel load, only becoming *visible* because this module's own heavy test suite now shares the same crate — fixed in place (wider retry budgets, tolerating an expected `BrokenPipe` when a server correctly rejects-and-closes promptly). See ADR-0049. Full `fossh-admin` crate (31 tests) and full Cargo workspace re-run clean 3 consecutive times at `--test-threads=32`; `fossh-ffi`'s separate workspace re-checked, unaffected. **Task #31 (wiring into real running services) is now substantially done — see ADR-0050 for full detail.** The bootstrap handoff was extended to carry full certificate PEM content in both directions (previously fingerprint-only, one-directional); a real bug surfaced by testing, not review, in the process — an OCaml `Sys_error "Connection reset by peer"` during `bootstrap.ml`'s reply read escaped as an uncaught exception instead of the module's own declared `Result` type, fixed with a new `Recv_failed` variant. Both real binaries now actually run this channel: `watchdog/bin/main.ml`'s real supervision-mode startup spawns a background thread (`watchdog/quic/quic_command_server.ml`, new) that serves QUIC connections and dispatches verified `restart`/`reload` commands to a new `Supervisor.request_termination`; `crates/fossh-fcgi/src/quic_client.rs` (new, feature-gated — see below) spawns a matching background thread that waits for the bootstrap handoff, then sends one command per real `SIGHUP` delivery over a fresh connection each time. A second real bug, also found only by running the code: the session hello was originally sent on QUIC stream 0, which failed immediately with a real `QUICHE_ERR_INVALID_STREAM_STATE` — stream IDs encode who may speak first on them (client- vs server-initiated) and this is enforced by quiche itself, not convention; fixed by moving to stream 1, the first server-initiated bidi ID. `fossh-ipc` (and the quiche/BoringSSL build it pulls in) stays out of ordinary builds: `fossh-fcgi` gained a new `quic` Cargo feature, off by default, with `fossh-ipc` as an optional dependency only resolved when it's explicitly enabled — verified directly that a plain `cargo build -p fossh-fcgi` still compiles none of it. Core's own half of the command-protocol wire format didn't exist in Rust at all before this pass (confirmed by search, not assumed) — new module `crates/fossh-admin/src/command_client.rs` (8 tests) fills that gap, deliberately kept in the lightweight, already-workspace-member `fossh-admin` rather than the heavy `fossh-ipc`. Full new real, end-to-end test (`watchdog/test/test_quic_command_server.ml`, 4 checks): a genuinely valid session-bound command over a real QUIC/mTLS connection delivers a real `SIGTERM` to a real spawned child process, and a wrong-token command is refused with the child confirmed still alive — not just the wire-level rejection. Deliberate, documented scope cuts (not silently assumed): `Restart` and `Reload` both dispatch to the same full-respawn action for now (no live config hot-reload exists in `fossh-fcgi` to reload *into*); only `Reload` has a live trigger; the separate §2.1 human-operator GPG challenge-response auth gate remains exactly as open as it already was, not conflated with this channel. **Adversarial review of this whole wiring pass found and fixed 2 CRITICAL, 1 HIGH, 2 LOW real bugs — full detail in ADR-0050, not repeated here.** Most seriously: `handle_one_connection` could raise an uncaught exception (a real, reproduced `/dev/urandom` fd-exhaustion case) that permanently killed the QUIC server thread with no further logging, fixed with a catch-all around the connection-handling call; and — the single most severe finding — a verified `Reload`/`Restart` command fed the *same* crash-storm counter a genuine crash loop needs, and reproducing 6 ordinary, individually-valid reload commands in a row (exactly what 6 real `SIGHUP`s produce) made the real watchdog process exit and stop supervising core entirely, not just refuse one restart. Fixed with a new `Supervisor.termination_was_requested` flag routing command-triggered restarts through a new, still-tamper-checked-but-not-storm-guarded `restart_after_requested_termination`, verified with a dedicated test driving 8 real cycles (more than the 5-restart cap) end to end. Also fixed: the identical uncaught-`Sys_error`-on-a-`Stdlib`-channel-op gap already closed once in `bootstrap.ml`, recurring in `core_pin.ml`'s write path (reproduced via a forced `EFBIG`); a real `str::trim()`-vs-`String.trim` divergence between the two "identical" wire parsers; a busy-loop-with-no-backoff on a persistent `accept_one` failure. The review's own strong recommendation — do not defer the missing cross-language test for this specific channel, given the exact stream-ID bug above was already an interop-only bug neither side's own suite could have caught, and the `trim()` finding is a second live example of the same undetected-drift risk — was acted on immediately: a new `crates/fossh-fcgi/tests/quic_command_interop.rs` spawns the real compiled `fossh-watchdog` binary (real `gpg`-generated key, real `gpg --clearsign`ed manifest, real supervised `/bin/sleep`) and drives core's real `quic_client::send_one_reload` against it, asserting both a verified `OK` reply and the watchdog's own log confirming dispatch — 3 consecutive clean runs, ~21-33s each (real process startup and `gpg` keygen dominate that time). Full watchdog suite now 11 binaries, 130 checks, clean; full Cargo workspace (default and `--features quic`) clean. |
| 3.5 | Privilege separation enforcement | done | The real `fossh-svc`/`fossh-watchdog` system users now exist on the dev machine (created by the user via the surfaced `useradd`/`groupadd` commands, matching `packaging/rpm/fossh.spec`'s own `%pre` exactly). New `scripts/verify-privilege-separation.sh` (must run as root — creates/chowns `/var/lib/fossh` and `/var/lib/fossh-watchdog` matching §2.3's model, since the RPM spec itself only creates the former today, no systemd unit/packaging exists for the watchdog yet, a separate already-tracked gap — then uses `runuser`, not `su`, since the target accounts are `/sbin/nologin` and `runuser` doesn't need a real login shell or PAM session for that) actually tests the boundary with real filesystem operations from both real users, not simulated ownership: `fossh-svc` cannot read, list, create a file in, or overwrite anything in `fossh-watchdog`'s directory; `fossh-watchdog` cannot write into `fossh-svc`'s directory either (checked both directions — prrr.md §3.5's own wording is one-directional, but a watchdog able to freely rewrite core's data would be a real, separate gap); a sanity check confirms `fossh-watchdog` *can* read its own file, proving the negative checks fail for the right reason. Run for real by the user (root access needed, no TTY available for `sudo` through this session's own shell) — all 6 checks passed. §3.5's second requirement ("`fossh-svc` cannot invoke a restart of itself or bypass the watchdog to talk to the auth gate directly") is satisfied by code audit rather than a runtime test: a direct search of `fossh-fcgi`'s/`fossh-cgi`'s own source confirms no self-exec/self-restart code path exists anywhere (the only way core is ever restarted is by the watchdog, via `Supervisor.spawn`), and the separate §2.1 auth-gate network listener this would bypass remains unbuilt (already documented, open item) — nothing exists yet for `fossh-svc` to reach around the watchdog *to*. |
| 3.6 | Tamper detection | done via §3.3 | Not actually blocked on a separate "shared Rust crypto crate" as this row previously (inaccurately) said — §3.3 ended up pure OCaml + `gpg` shell-outs, no Rust crypto crate involved at all. What §3.6 actually asks for is already built and tested as part of §3.3: `Manifest` (signed manifest of expected binary+config hashes, `sha256sum`-based, clearsigned with the watchdog's own key — now `Keypair`-generated and reused, ADR-0043), verified on every restart *and* the initial launch (ADR-0041 fixed the initial-launch gap). The "define explicitly what 'tampered' triggers" requirement is answered and documented (ADR-0040/ADR-0041, and this file's own §3.3 retrospective): refuse restart, not restart-and-log — fail closed. One honest nuance: "alert" today means `log` to stderr (→ the systemd journal, once this runs under a real unit) plus a distinct non-zero exit code (3) an operator or monitoring tool can key off — not an active push notification (email/webhook); the spec doesn't explicitly require the latter, and none exists yet. |
| 3.7 | CGI input trust boundary + fuzzing | implemented, verified | Trust-boundary centralization: `fossh_ingest::ingest::decide()` (built during M7) is the one place auth/rate-limit/pipeline-dispatch decisions get made, shared by `fossh-cgi` and `fossh-fcgi` rather than duplicated per transport. `crates/fossh-fcgi` split into a `[lib]` + thin `[[bin]]` so its `protocol`/`connection` modules are reachable from outside the crate; `fuzz/` (excluded from the root workspace — cargo-fuzz needs nightly + its own profile overrides that collide with the shared `panic = "abort"` release profile) wired against five real `cargo-fuzz` targets matching the spec's testing table: `json_body`/`query_string` (wire parsers, fixed `RequestContext` + non-empty allowlist so inputs get past the first check), `forwarded_for` (`resolve_client_ip`, `arbitrary`-derived structured input), `fcgi_framing` (`read_header` → `read_record_body` → `decode_name_value_pairs`, the least-trusted bytes in the binary), and `spool_frame` (`decode_event`, the plaintext frame parser, deliberately not the ChaCha20-Poly1305 layer around it — see ADR-0037 for why). **No longer blocked**: `gcc-c++` installed (user ran the `dnf install` themselves — ADR-0023's password rule held: it was surfaced as a command, never typed into a tool call by this session). All five targets actually built and ran (`cargo +nightly fuzz run <target> -- -max_total_time=15`): 1.9M, 1.2M, 9.8M, 5.7M, and 5.5M executions respectively in 15 seconds each, zero crashes, zero `fuzz/artifacts/` files left behind. **Long unattended run done**: each of the 5 targets re-run for a real 900s (15 min), sequentially, ~76 minutes total wall-clock — `json_body` 49.2M, `query_string` 29.0M, `forwarded_for` 494.6M, `fcgi_framing` 201.0M, `spool_frame` 160.1M executions, **933.9M total, zero crashes, zero `fuzz/artifacts/` files** across all five. **CI wiring done**: a `fuzz-smoke` job (15s/target, all 5) now runs on every push/PR — see this file's top entry. |
| 3.8 | Data-at-rest encryption | implemented (spool only) | Spool frames sealed with ChaCha20-Poly1305 under a per-install key; the rollup SQLite database is deliberately not encrypted this pass — see ADR-0027 for the reasoning (it holds aggregates, not raw rows). |
| 3.9 | Local TUI admin console | QA-gate passed | `crates/fossh-tui`: dashboard, real telemetry-summary screen reading straight from `fossh-store`, and the §2.6/§3.11 setup wizard flow. Adversarial review found one high-severity functional bug (verifying a pre-existing token file could never succeed) plus five smaller findings — all fixed and re-verified. 18 tests (up from 8), clippy/fmt clean. See retrospective for the full list. **Since then (2026-08-06):** two more real screens/clients landed — `watchdog_status.rs` (live watchdog/tamper-detection query over §3.4's QUIC channel, dashboard no longer shows static "not wired yet" text) and `operator_auth_client.rs`/`operator_auth.rs` (§2.1's Rust-side challenge-response client, shelling to `gpg --armor --detach-sign`, real cross-language interop test against the compiled `fossh-watchdog`). 32 tests now (up from 18/21). **Since then, same day:** the open gap flagged right here (`wizard.rs` still standalone/demo-mode, never speaking the real `Setup_token`/`Operator_auth_server` SETUP protocol) is closed — `wizard.rs` rewritten to drive the real protocol end to end (submit the real token, paste-or-generate a real key, real `ENROLLED`/`ENROLL_FAILED`/`SETUP_DENIED` states), `operator_auth_client.rs` gained the SETUP-flow client half plus a real interop test against the compiled watchdog, and a dedicated adversarial-review pass found and closed one HIGH (a client-side drip-attack wall-clock-deadline gap in both `setup_at` and the pre-existing `authenticate_at`) and two lower-severity findings. 59 tests now (up from 32). See DECISIONS.md's ADR-0059 and this file's top entry for full detail. |
| 3.10 | Supply chain & build hardening | mostly done | `cargo audit` and `cargo deny check` (advisories/bans/licenses/sources) both genuinely clean in **both** workspaces — 242 + 72 crate dependency graphs, real runs. **Re-verified after task #31's QUIC wiring** (`fossh-fcgi`'s new optional `quic` feature makes `fossh-ipc`/quiche/BoringSSL resolvable — though not built by default — in the main workspace's own `Cargo.lock`, growing its dependency graph to 277): both re-run clean, same "advisories ok, bans ok, licenses ok, sources ok" result, confirming the new dependency tree didn't quietly introduce anything this gate exists to catch. Stripped-release-symbol verification: `file` reports `stripped` and plain `nm` reports "no symbols" on all 6 shipped artifacts (`fossh`, `fossh-cgi`, `fossh-tui`, `fossh-fcgi`, `libfossh.so`, `libfossh.a`); `nm -D` on `libfossh.so` confirms the intended public C ABI (`fossh_init`/`fossh_event`/`fossh_pageview`/... ) is still exported via the dynamic symbol table, not accidentally stripped too. Reproducible build: built the canonical way (`scripts/build-release.sh`, not a bare `cargo build`) twice from a clean `target/`, diffed SHA-256 of all 6 artifacts both times — byte-identical, and zero `/home/` path leaks confirmed both via `strings` directly and `scripts/check-identity-hygiene.sh`. `opam` audit: reached now that real OCaml source and a real switch exist (§3.3), and the honest finding is that there is no automated equivalent to `cargo audit`/RustSec for opam — checked directly (`opam show opam-audit` → "No package matching opam-audit found"; no `audit`/`advisory` subcommand exists in opam 2.5.2's own command list), not assumed absent. What's actually possible instead, and was done: a full, explicit dependency inventory (`opam list` in the watchdog switch) — 9 real third-party packages once the virtual/compiler-provided ones are excluded (`ctypes`, `ctypes-foreign`, `dune`, `dune-configurator`, `conf-libffi`, `conf-pkg-config`, `csexp`, `integers`, `stdlib-shims`), every one a well-known, foundational, actively-maintained part of the OCaml ecosystem (`dune` and `ctypes` specifically are closer to "the standard tool for this" than obscure choices), small enough to review by hand — which is itself the point, matching §3.3's own explicit "keep this component's dependency tree minimal" design goal. **The "zero `/home/` path leaks" claim above turned out to be narrower than it read (ADR-0054):** it covered the 6 `scripts/build-release.sh`-built Rust artifacts, correctly, but not the RPMs (a separate `rpmbuild`-driven leak, see §3.11's row) or `libquiche.so`/`libquiche.so.0` — a real, reproduced 144-hit leak from BoringSSL's own C/C++ sources, which `--remap-path-prefix` (rustc-only) never reached; fixed with `CFLAGS`/`CXXFLAGS -ffile-prefix-map` added to `scripts/build-quiche-ffi.sh`. `scripts/check-identity-hygiene.sh`'s own sweep regex also had a real gap (required a trailing slash after the username, missing a leak already sitting in this file's own prose) — fixed, re-verified against the whole repo with no new false positives. |
| 3.11 | RPM packaging & setup wizard | core packaging done | `packaging/rpm/fossh.spec` builds a real, complete `fossh-0.1.0~alpha.1-1.fc44.x86_64.rpm` + matching `.src.rpm` end to end on this box (`dist/`, gitignored) via a real `git archive`-built source tarball and a full `rpmbuild -bb`/`-bs --nodeps` (`--nodeps` because this environment's Rust is rustup-managed, not the system `cargo`/`rust` RPMs the spec's `BuildRequires` names — a real, deliberate mismatch, not an oversight). Added a `%check` section (`cargo test --workspace` + `fossh-ffi`'s own suite) that now genuinely runs as part of the build, not just at %build time — confirmed by watching it actually execute and pass during a real rebuild. `rpmlint` now installed and run for real against both the spec and the built packages: fixed a real gap (`post-without-tmpfile-creation` — `/run/fossh`'s tmpfiles.d entries wouldn't exist until the next reboot without an explicit `systemd-tmpfiles --create` in `%post`) and cosmetic spec noise (`macro-in-comment` ×4, unescaped `%`/`%{}` in prose comments). Left alone, with reasons: `invalid-url` on `Source0` (no real upstream release URL exists yet — a fabricated one would be worse), `no-manual-page-for-binary` ×4 (real gap, genuinely deferred, not silently skipped), `non-standard-uid/gid` and `tmpfile-not-in-filelist` (both expected/correct for a package that creates its own system user and uses tmpfiles.d as designed), `dangerous-command-in-%post/%postun/%posttrans` (rpmlint flagging `rm` inside Fedora's own standard `%selinux_modules_install`/`%systemd_post`-family macros, not anything this spec wrote directly), and every `spelling-error` (rpmlint's dictionary not recognizing `cgi`/`fcgi`/`tui`/`systemd`/`md` — not misspellings). `%post` still leaves the service enabled but not started (awaiting-setup, per §3.11's own wording). **The setup wizard's full "enroll a real key" step is now done**: it depended on the watchdog's §2.6 setup-token gating (§3.3, ADR-0057) and `wizard.rs` actually speaking that protocol (ADR-0059) — both landed since this line was first written; see this file's top entry. **A real identity leak found and fixed while preparing task #26's public release (ADR-0054):** the shipped RPM and SRPM both had the real build machine's username and hostname baked into package metadata (`rpmbuild`'s own `%_topdir`/`%_buildhost` defaults, not anything the spec itself does wrong) — 13 and multiple real `strings` hits respectively, confirmed and fixed by building with `--define '_topdir ...' --define '_buildhost ...'` overriding both. `dist/`'s RPMs regenerated clean and reverified individually. A real, tracked follow-up: this override isn't yet automated into a script the way `scripts/build-release.sh` automates the equivalent for plain `cargo build` — a manual step for now, not silently assumed handled. **Since then (2026-08-06):** that follow-up is done — `scripts/build-release-rpm.sh` (still untracked in git, a separate real gap ADR-0056 already flagged and this entry doesn't close) now exists and automates the `_topdir`/`_buildhost` override for real. `packaging/systemd/fossh-watchdog.service` added (see this file's top "Last updated" entry for the `JoinsNamespaceOf=` reasoning and the `/var/lib/fossh-watchdog` ownership gap it also closed). A real clean-VM install (first ever, not just `rpmbuild --nodeps`) found and fixed 3 more real bugs — missing `Provides: user()/group()`, unexpanded `gen_context()` in `packaging/selinux/fossh.fc`, and the untracked-files gap in the tarball-snapshot script — full detail in this file's top entry. **Since then (final rebuild, this file's top entry): `dist/`'s RPM/SRPM were rebuilt fresh** (now include the watchdog unit, `Setup_token`, the `boring-sys` fix, and `wizard.rs`'s rewiring) and independently re-verified — `strings` clean across every shipped binary, RPM/SRPM confirmed byte-identical to the copies bundled inside the rebuilt public zip. No longer stale. |

## Locked decisions (§2) — acknowledged, not re-litigated

Restated here only so a reader of this file doesn't have to cross-reference the chapter doc to know what's already settled: challenge-response auth gate with session tokens (§2.1); OCaml binds to `quiche` via `ctypes` rather than a pure-OCaml QUIC stack (§2.2); `fossh-watchdog`/`fossh-svc` as two separate system users (§2.3); glibc floor (candidate 2.17), dynamically linked, hard-refuse below floor — musl is no longer the target for this deployment tier (§2.5, supersedes the original spec's musl-first framing for this tier only — CGI/FFI deployment shapes elsewhere are unaffected); per-install watchdog keypair, pinned on both sides, Unix-socket bootstrap handoff (§2.4); one-time setup token, plaintext written once to a fixed path, only its hash stored, burned atomically on success (§2.6).

## Environment facts (this machine, recorded so decisions above aren't second-guessed later)

- Fedora Linux 44 (COSMIC). `dnf`, `rpmbuild` (6.0.2), `semodule`, `checkmodule`, `gcc`, `pkg-config` present. As of this update, also present: `gcc-c++` (16.1.1), `ocaml` (5.4.0), `opam` (2.5.2), `dune` (on PATH, bundled with the ocaml/opam install rather than its own RPM), `selinux-policy` (44.5), `rpmlint` (2.8.0) — the user ran the surfaced `dnf install` command themselves (see the sudo/password note below). `selinux-policy-devel` specifically (the RPM `BuildRequires` names) is still not installed — a naming miss on this session's part, not the user's; `selinux-policy` (base) landed instead. Doesn't block anything actually built so far: `checkmodule`/`semodule_package` (the real tools §3.2's SELinux module needs) were already present before this round. `cmake` (4.3.0) and `clang-devel`/`clang-libs` (22.1.8, for `libclang.so` — BoringSSL's own `bindgen` step needs it) are now both installed too, also by the user; `quiche` (§3.4's dependency) now builds cleanly end to end, real BoringSSL compile included, confirmed with a real `cargo build --features ffi`.
- Rust: `rustup` with `stable` (active) and `nightly` toolchains both already installed; `cargo`/`rustc` 1.97.1 (stable), 1.99.0-nightly. `cargo-fuzz` 0.13.2 installed. `~/.cargo/bin` is not on `$PATH` in non-interactive shells in this environment — every command in this chapter that needs cargo/rustc explicitly prepends it.
- Network egress from `cargo`/`rustc` works (verified with a real `crates.io` dependency fetch, `ratatui` and its full tree). A bare `curl` to `crates.io` gets an HTTP 403 from its WAF (user-agent based, unrelated to `cargo`'s own client) — not a sign network is actually blocked, just not the way to test it.
- No passwordless `sudo` configured (`sudo -n true` fails). Per ADR-0023, the password given at the start of the original session — and given again twice, unprompted, later in this same chapter — is never placed in a command; where root is genuinely required, the exact command is surfaced here and in-chat for the user to run themselves. This is exactly how `gcc-c++`/`ocaml`/`opam`/`dune`/`selinux-policy`/`rpmlint` above actually got installed: surfaced as one `dnf install`, run by the user via Claude Code's own `!`-prefix mechanism, password never touching this session at any point.
- `rpmbuild`'s `BuildRequires` dependency check (`cargo`, `rust`, `selinux-policy-devel`) doesn't resolve against a rustup-managed toolchain even though it works fine — expected, not a bug in the spec. Real RPM builds in this environment use `rpmbuild -bb/-bs --nodeps`, letting the actual installed tools (rustup's cargo/rustc, the already-present `checkmodule`/`semodule_package`) do the real work while skipping rpm's own package-database-only metadata check.
- `ocaml-ctypes`/`ocaml-ctypes-devel`/`ocaml-findlib`/`ocaml-findlib-devel`/`ocaml-integers`/`ocaml-integers-devel` (§3.4's OCaml side, `watchdog/quic/`) are **not yet installed** as Fedora RPMs on this machine, confirmed available in the Fedora repos (`dnf list --available`) but not installed — surfaced, not blocked on: `watchdog/quic/`'s own development and verification this pass used a local opam switch already present at `watchdog/_opam/` (leftover from earlier exploration, `ctypes`/`ctypes-foreign` 0.24.0 — the exact same version the Fedora RPM ships, confirmed via its `META` file) with the *system* `ocaml`/`ocamlopt` compiler on `PATH` rather than an opam-managed one, matching this project's established "no opam in the real build" preference as closely as possible while still getting real, working verification now rather than waiting. `packaging/rpm/fossh.spec`'s `BuildRequires` already lists the real Fedora package names for when a canonical rebuild against them happens.

## §19.4.12 identity-hygiene CI gate

`scripts/check-identity-hygiene.sh` + `scripts/test-identity-hygiene-gate.sh` + `.github/workflows/ci.yml` — see ADR-0031 and its follow-up entry (a real debugging pass against this actual repository, not just the isolated fixture, found and fixed three genuine self-referential false-positive bugs — including one *in the fixture itself*, where the "safe" example wasn't actually shell-safe). Verified locally, both directions, after every fix — not yet run through real GitHub Actions infrastructure, which doesn't exist for this repository yet.

## §19.7 release zip

`scripts/build-release-zip.sh` produces `dist/fossh-oa.zip` per the required layout (`readme.md`/`tos.md`/`SHA256SUMS` at the zip root, the full repo under `fossh/`). Built twice independently and diffed the SHA-256 — byte-identical, reproducibility genuinely verified. `dist/`'s contents reflect what this build environment can actually produce (the glibc release binaries, the RPM/SRPM, `libfossh.{so,a}`, `fossh.h`) rather than the full §19.7 wish list (musl ×2 arches, an SBOM, a signed manifest) — those remain open, tracked here rather than silently implied.

## Retrospectives

(Appended one entry per sub-chapter as its QA gate closes.)

### §3.8 — Data-at-rest encryption

Scoped to the spool specifically, not the rollup SQLite database — see ADR-0027 for the full reasoning (the database holds k-anonymity-folded aggregates; the spool is where genuinely raw, per-visitor telemetry actually sits on disk, even if briefly). Built: `fossh_admin::data_key` (per-install key, race-safe `load_or_generate`, 5 tests including 8 real concurrent threads racing a cold-start), `fossh_ingest::crypto` (ChaCha20-Poly1305 seal/open, 7 tests covering the round trip, wrong key, tampered ciphertext, and truncation), and wiring through all three transports that touch the spool: `fossh-cgi` (loads the key once per process, after the privilege drop, before the `/healthz` fast path would ever need it), `fossh-cli maintain`, and `fossh-ffi`'s `mode = "spool"` path (`record`/`fossh_flush`) — confirmed by running `fossh-ffi`'s own `flush_drains_the_spool_in_spool_mode` test, which exercises the modified code directly, not just a unit test of the crypto primitive in isolation.

Added two integration-level tests beyond the crypto module's own unit tests: one asserts the literal on-disk spool file bytes don't contain a distinctive plaintext path/name marker (proving encryption actually happens at the file level, not just in a round-trip assertion that could pass even if encryption were accidentally a no-op), and one confirms draining with the wrong key reports every frame `Corrupt` rather than panicking or silently producing wrong data.

Full workspace (both the root and `fossh-ffi`'s separate one) still green after the change: 280 + 32 = 312 tests, clippy clean, fmt clean in both. The RPM built for §3.11 predates this change — rebuilding it to pick up the new code is a follow-up, not done as part of this entry.

### §3.1 — glibc gatekeeping

Built: `fossh_core::glibc_gate` (pure `ldd --version` parsing/comparison, 7 tests), `fossh_cli::common::detect_glibc_version` (the one I/O-performing caller both `doctor` and the new `fossh glibc-check` subcommand share), and the floor constant (2.17, per §2.5's candidate). Deliberately not wired into `fossh-cgi`'s per-request path — see ADR-0024 for the full reasoning (subprocess-per-request would tax the sub-5ms p99 budget for no benefit, since the host's glibc can't change request-to-request).

Adversarial pass caught one real, moderate-to-high-severity bug: neither call site checked `ldd`'s exit status before trusting its stdout, so a failing `ldd` (e.g. a half-broken glibc install — precisely the failure mode this check exists to catch) that still printed something version-shaped on stdout parsed as a clean pass. Confirmed empirically against the compiled binary with a shadowed fake `ldd` on `$PATH`, not just by reading the code. Fixed by collapsing both call sites onto one shared helper that checks `status.success()` first — see ADR-0024. Re-ran the adversarial repro against the fix directly; it now correctly refuses.

Also caught and fixed: a doc comment in `glibc_gate.rs` referenced an ADR that didn't exist yet (fixed by writing ADR-0024 for real, not by softening the comment) and a dangling `RULES.md` pointer in the refusal message (RULES.md has no glibc content). That refusal message no longer points anywhere at all as of the `dev/` reorg below — see that entry for why.

No deviation from §2.5's locked decision. One thing worth being explicit about, flagged in ADR-0024 too: the check exists but isn't automatically invoked by anything yet — that lands with §3.2's systemd unit (`ExecStartPre=`).

### §3.2 — Native CGI hardening

Built: privilege-drop (`fossh-cgi/src/privdrop.rs`, using `nix`'s safe wrappers, never touching this crate's `#![forbid(unsafe_code)]` — setgid → initgroups → setuid, then verifies the drop actually stuck by attempting to reclaim root); the SELinux module (`packaging/selinux/fossh.{te,fc}`, written from scratch in raw native TE syntax since this environment has `checkmodule`/`semodule_package` but not `selinux-policy-devel`'s M4 interface library — see the file's own header comment); and the systemd units (`packaging/systemd/fossh-fcgiwrap.{socket,service}`, `fossh.tmpfiles.conf`) with the full hardening directive set (`PrivateNetwork`, `ProtectSystem=strict`, `NoNewPrivileges`, capability/syscall restriction, etc.) plus `ExecStartPre=fossh glibc-check`.

Verified as far as this rootless environment allows: `checkmodule`/`semodule_package` both compile and package the policy module cleanly (real compile, re-run against the final file content, not just written-and-hoped); `systemd-analyze verify` parses both units cleanly (only complaining that `/usr/bin/fossh`/`/usr/sbin/fcgiwrap` aren't installed system-wide here, which is expected pre-install). Not verified here, needs root: actually loading the SELinux module (`semodule -i`) and testing real AVC enforcement against a live process; exercising the privilege-drop code's actual-root-successfully-dropped path (this session never runs as root, per ADR-0023); creating the real `fossh-svc`/`fossh-watchdog` system users the units and policy both assume exist (that's §3.5/§3.11's job). Adversarial-review subagent pass launched; retrospective will be updated with findings once it returns.

Also fixed, while working on this: the earlier preview nginx+Cloudflare-Tunnel guide (`docs/preview/DEPLOY-nginx-cloudflare-tunnel.md`) referenced a hand-rolled socket path (`/run/fcgiwrap-fossh.sock`) that no longer matches the real shipped unit (`/run/fossh/fcgiwrap.sock`) — updated that guide to reference the real unit files directly instead of a duplicated hand-rolled copy, and added the `real_ip_header`/`set_real_ip_from` nginx config it was missing (without it, every visitor behind that guide's Cloudflare Tunnel setup would have collapsed to `127.0.0.1` as far as `fossh-cgi` could tell — cloudflared's own loopback hop to nginx, not the actual visitor).

**Adversarial review findings and fixes.** `privdrop.rs` and the systemd units held up well — the reviewer traced the setgid-before-setuid ordering, `initgroups`' full (not additive) group replacement, and the reclaim-root verification check against actual Linux/`nix`-0.29.0 semantics and found them sound, plus confirmed the hardening directives (`RestrictAddressFamilies=AF_UNIX`, `PrivateNetwork`, `ProtectSystem=strict` + `ReadWritePaths`) actually match what the code touches, cross-referenced line by line against `handler.rs`/`salt.rs`. One low-severity nit noted (the reclaim-check doesn't discriminate on errno) — not fixed, narrow enough not to be worth the complexity.

The SELinux module was a different story: the reviewer correctly pointed out that "compiles cleanly" (this project's own verification claim) gives **no assurance at all** about missing `allow` rules, since `require{}` blocks aren't resolved against a real base policy until load time — and found three concrete ones by checking this box's actual labels (`ls -Zd`) and cross-referencing actual code instead of the policy's own assumptions:

- **Missing ancestor-directory `search` grants** for `/etc` (`etc_t`), `/usr` (`usr_t`), `/var` (`var_t`), `/var/lib` (`var_lib_t`) — SELinux checks `dir:search` at every path component, so without these the dynamic linker likely can't even resolve `libc`/`ld.so.cache` and the domain can't reach its own data directory. Likely would have broken every single exec. **Fixed**: added explicit `search`/`read`/`getattr` grants for all four, plus `lib_t:dir` (the directory form of a type this module already required, previously only granted `file`/`lnk_file` access on).
- **`/dev/urandom` (`urandom_device_t`) never granted at all** — confirmed via `fossh-ingest/src/random.rs` that this is opened directly (not `getrandom(2)`) on the hot path whenever the daily salt rotates. **Fixed**: added `chr_file { read write getattr open ioctl }` on `urandom_device_t`.
- **`capability { setuid setgid }` was `require`d but never actually `allow`ed** — would have blocked `privdrop.rs` under enforcement in the one scenario it exists for (this binary genuinely exec'd as root). **Fixed**: added `allow fossh_cgi_t self:capability { setuid setgid };`.

Also addressed, not a bug but a real architectural gap the reviewer flagged and I couldn't resolve without root: the shipped systemd unit execs `fcgiwrap`, not `fossh-cgi`, directly — `fcgiwrap` itself execs `fossh-cgi` per request, and whatever domain `fcgiwrap` actually runs in (the unit sets no `SELinuxContext=`) might not be `init_t` or `httpd_t` at all; Fedora commonly lands such units in `unconfined_service_t`, in which case the transition never fires and `fossh-cgi` runs completely unconfined, silently. Added `unconfined_service_t` as a third source domain for the same transition, as a defensive best guess — but the actual answer (`ps -eZ | grep fcgiwrap` after a real `semodule -i` + service start) is still open and needs root to confirm; tracked here rather than glossed over.

While fixing the above, also trimmed genuinely unused permissions the reviewer traced through the actual ingest code (`lock`/`map` on `fossh_data_t`/`fossh_var_run_t:file` — nothing on this path uses `mmap()` or advisory locking, per ADR-0013), removed redundant `entrypoint`/`execute_no_trans` grants on the source domains' view of the executable (the check only consults the target domain's relationship, already granted separately), and corrected a stale comment claiming `fossh_data_t` represents the SQLite database — it doesn't; `fossh-cgi` never opens `fossh.db` directly, only the JSON site-cache and per-site spool/ratelimit/nonce files. Re-verified with `checkmodule`/`semodule_package` after every fix — still compiles and packages cleanly. The nginx+tunnel doc's socket-path/`real_ip` drift the reviewer also flagged had already been fixed earlier in this same session, before the review returned — confirmed no residual drift remains.

### §3.11 — RPM packaging

Wrote `packaging/rpm/fossh.spec` (no `rust2rpm`/Fedora Rust-packaging macros — this environment doesn't have that package, so `%build`/`%install` are plain `cargo build --release --workspace` + manual `install -D`, a valid if less-automated alternative) and got a **real, complete `rpmbuild -bb` to succeed** — not just `rpmspec --parse` or a source-RPM check. `~/rpmbuild/RPMS/x86_64/fossh-0.1.0~alpha.1-1.fc44.x86_64.rpm` exists, is a real ~1.5 MB package with correctly auto-detected glibc/libgcc/libm `Requires:`, correct `%pre`/`%post`/`%preun`/`%postun`/`%posttrans` scriptlet expansion (verified by reading `rpm -qp --scripts` output, not assumed) using the real `%selinux_modules_install`/`%selinux_relabel_post`/`%systemd_post` macros this system actually ships, and the right file manifest (`rpm -qlp`). Copied into `dist/` (gitignored) alongside the source RPM.

Two honest gaps, not hidden: `rpmlint` isn't installed here, so no static-lint pass beyond what `rpmbuild` itself checks; and `--nodeps` was needed for the `-bb` run because this dev box provisions its Rust toolchain via `rustup`, not the distro's `cargo`/`rust`/`selinux-policy-devel` RPM packages — on an actual Fedora build host (or Koji/mock) with those installed, the same spec should build without that flag. Also not yet done, and explicitly out of this spec's scope until it exists: `cargo vendor`-based offline building for real network-isolated Koji/mock builds (tracked under §3.10).

### X-Forwarded-For trust + shared-hosting PHP HTTP-remote mode (user request, mid-chapter)

Not a numbered §3.x sub-chapter — a direct user request to make the write key ("API key") actually usable from real shared hosting, where `ext-ffi` and `proc_open` are both commonly unavailable. Added: `FOSSH_TRUST_FORWARDED_FOR` (opt-in, off by default — `fossh_ingest::forwarded`, 5 tests, ADR-0025; moved from `fossh-cgi` to `fossh-ingest` during M7, see that entry) so a trusted relay's forwarded IP can be honored without any deployment silently trusting a spoofable header by default; a third transport in `bindings/php/src/Client.php` (HTTP-remote, bearer-token auth over plain HTTPS via `curl` or PHP's stream wrapper, whichever is available, ~2s timeout, never throws); `docs/INTEGRATION-php.md` covering API-key creation, the shared-hosting HTTP mode, why `FOSSH_TRUST_FORWARDED_FOR` matters for correct per-visitor counts in that mode specifically, a WordPress snippet, and a short note that running alongside Google Analytics/GTM needs no special handling (no cookies, no client-side script, nothing to conflict with) — also reflected in `RULES.md` §5. Verified: full Rust suite (250 tests, root workspace) + `fossh-ffi`'s separate workspace (32 tests) all pass, clippy/fmt clean. The PHP client itself could not be run against a real PHP interpreter (none installed in this environment, same limitation as the rest of M6) — reviewed by hand instead; see the file's own comments for the specific `curl`/stream-context/JSON-shape reasoning.

### Repository reorg — `dev/` and `docs/preview/` (user request, mid-chapter)

Not a numbered §3.x sub-chapter either. The user's own complaint: inline "Status: preview, deferred, not through adversarial review yet" banners were scattered through otherwise-clean public docs, and private/in-progress/internal-process material was tangled together with polished public documentation in one flat folder. Requested fix: communicate that distinction through directory structure instead of repeated disclaimer paragraphs.

Done: `git mv DURUM.md dev/DURUM.md` (this file); `git mv docs/DEPLOY-nginx-cloudflare-tunnel.md docs/preview/DEPLOY-nginx-cloudflare-tunnel.md` (the one deployment tier that hasn't been through this project's adversarial-review QA gate — every other `docs/*.md` guide has). The inline banner at the top of that guide was then shortened, since the `docs/preview/` location now carries most of the signal on its own — it no longer needs four sentences to say what one location-implied sentence covers, and it no longer cites `prrr.md`'s own §6, which isn't shippable/checkable by a reader in the first place (that source spec lives outside the repo, in the author's local downloads folder, never committed).

Every cross-reference to both old paths was then found via `git grep -l` (14 files for the `DURUM.md` rename, 6 for the nginx guide) and fixed file by file — `RULES.md`, `THREAT_MODEL.md`, `NOTICE`, `readme.md`, `docs/INTEGRATION-php.md`, `docs/DEPLOY-caddy.md`, `packaging/selinux/fossh.te`, `packaging/systemd/fossh-fcgiwrap.service`, `scripts/build-release-zip.sh`, `crates/fossh-cgi/src/privdrop.rs`, `crates/fossh-cli/src/commands/glibc_check.rs`, `crates/fossh-tui/src/{main,ui,wizard}.rs`, `DECISIONS.md`, `README.md`/`README.tr.md`, and this file's own two self-references. One deliberate exception: `DECISIONS.md`'s ADR-0031 entry names `DURUM.md` (no `dev/` prefix) when describing the identity-hygiene leak found and fixed in it *before* this move happened — that's a historical fact about a past file state, not a live pointer, and rewriting it to `dev/DURUM.md` would misrepresent when the rename actually happened relative to that fix. Every other mention was a live citation meant to help a reader find the file *today*, so those were all updated.

Two real issues surfaced while doing this mechanical pass, not invented after the fact to pad it out:

1. `docs/INTEGRATION-php.md` claimed the Laravel/Symfony middleware wrappers were "planned but not shipped yet" — false; `bindings/php/src/Laravel/FosshMiddleware.php` and `bindings/php/src/Symfony/FosshRequestSubscriber.php` both exist and were completed as part of M6. Fixed to describe them as shipped, with their real paths.
2. `fossh-cli`'s `glibc-check` refusal message (the one printed to an operator's real terminal when a systemd `ExecStartPre=` gate fails on a below-floor host) cited `DURUM.md §2.5` directly. That's a runtime, end-user-facing string — pointing whoever just ran `sudo dnf install fossh` at an internal chapter-status tracker this same reorg just finished labeling "not end-user documentation" is a direct contradiction. Fixed by dropping the citation from that message entirely rather than repointing it; the message stands on its own without it. (By contrast, `fossh-tui`'s dashboard screen citing this file for "Watchdog: not running" status was left as a citation and just had its path fixed — that's a local admin console screen for the operator actively running the tool, a meaningfully different, more technical audience than a hard-failure CLI error, and the citation is genuinely actionable for them since this file ships with the repo.)

Re-verified after every fix, not assumed clean: `scripts/check-identity-hygiene.sh` (paths changing shouldn't matter since it operates on `git ls-files`, but re-ran it anyway) and `scripts/test-identity-hygiene-gate.sh` both still pass in both directions; full workspace `cargo test` (root: 13+17+21+96+83+42+8 = 280 tests, `fossh-ffi`: 32 tests, all passing, 0 failed) plus `cargo clippy -- -D warnings` and `cargo fmt --check` clean in both workspaces.

While re-reading this file end to end during the reorg (checking every remaining self-reference), found the §3.9 status table row still said "not started" despite `crates/fossh-tui` existing, being committed, and having 8 passing tests — the code was written but never actually taken through this project's own mandated QA gate. See the next retrospective entry for the review that was missing and what it found.

### §3.9 — Local TUI admin console — QA gate closed out retroactively

`crates/fossh-tui` (dashboard, real telemetry-summary screen, §2.6/§3.11 setup wizard) was implemented and committed (`8f71713`) in an earlier entry in this same chapter, but — unlike every other sub-chapter above — never actually went through this project's own mandatory sequence: implement → adversarial review → fix → retrospective → this table updated. It sat at "not started" in the status table the whole time despite being real, tested, committed code. Caught only by chance, re-reading this file end to end during the directory reorg above. Ran the missing review now, independently, rather than just correcting the table entry cosmetically.

**Adversarial review findings, all independently verified (build/test/clippy re-run, not assumed) and fixed:**

1. **High severity — verifying a pre-existing token file could never succeed.** `Wizard::new` set `pending_hash: None` unconditionally regardless of whether a token file already existed, and only `generate()` (reachable only from `NoTokenFound`) ever populated it. So reopening the TUI between generating a token and verifying it — exactly the shape this becomes once §3.3's watchdog exists and always writes the file in a separate process from this one — left `submit()` permanently unable to succeed, no matter what was typed, with no in-app recovery path. The existing test suite didn't catch it: `existing_token_file_starts_at_awaiting_input` asserted only the starting `Step`, never attempted a full verify. Fixed by having `Wizard::new` read the file and re-derive the hash itself (new `fossh_admin::setup_token::hash_of`, the same primitive `generate()` already uses internally) rather than assuming a hash it never had. Regression tests added: a full generate-elsewhere-then-verify-here cycle, plus a wrong-input-against-a-recovered-hash case.
2. **Zeroization gap, real but defense-in-depth rather than actively exploitable.** The setup token's plaintext — both the freshly-generated copy (`Step::TokenJustGenerated`) and whatever the operator types while verifying (`Wizard::input`) — lived in plain, unzeroized `String`s. A wrong guess only reset length (`String::clear()` doesn't touch the underlying bytes), and a *correct* guess never cleared the buffer at all. Worth fixing for consistency with this project's own bar elsewhere (`fossh-admin::data_key`'s `Zeroizing` usage) even though the token is already burned/invalidated by the time this matters, so there's no live credential left to steal this way — the actual exposure needs something else to go wrong first (a core dump, a ptrace read, a swapped page). One sharp edge the review caught that a bare `Zeroizing<String>` alone wouldn't have closed: the workspace's release profile sets `panic = "abort"`, which skips unwinding, so a `Drop` impl doesn't fire if the process panics while the plaintext is still live — the actual fix has to be *proactive* clearing the moment the value is no longer needed, not just wrapping it and trusting a clean shutdown. Fixed: both fields are now `Zeroizing<String>`, and `Wizard` explicitly zeroizes `input` right after a wrong guess, right after a successful verify, and on cancel (see next finding) — not left to whenever the struct eventually drops. Also fixed the render path in `ui.rs`, which had been `plaintext.clone()`-ing the generated token into a fresh, unprotected `String` on every single frame redraw (the event loop redraws roughly every 100ms while that screen is shown) purely to display it — switched to borrowing (`.as_str()`) instead, so rendering no longer keeps minting new unprotected copies of a live secret for as long as the screen stays up.
3. **The status line was actively lying, and there was no way to cancel out of token entry short of quitting the whole app.** The footer unconditionally advertised "q quit · 1/2/3 switch screen · r refresh" on every screen — but the raw-input-capture state (`Step::AwaitingInput`) routes every key, including q/1/2/3, into the token buffer instead, so none of those claims were true while actually typing a token. The only key that still did something in that state was Esc — and it quit the entire application, not just the wizard, with no mention of Esc in the footer at all and no other way back to a menu. Fixed: the footer is now context-sensitive (different, accurate hints per screen and per wizard step), and Esc during token entry now clears the input and returns to the dashboard instead of quitting.
4. **`FOSSH_K_ANONYMITY=0` silently defeated k-anonymity on this screen's own telemetry display.** The env var was parsed straight into `u32` with no floor check, so an explicit `0` sailed through — and `fossh-store`'s k-anonymity fold is `if uniques < k_anonymity`, meaning `k=0` folds nothing into `(other)` ever, directly contradicting the Telemetry screen's own "k-anonymized" label. `fossh-cli`'s `doctor` already names exactly this as a real invariant to check (`k_anonymity >= 1`); this TUI's own intake path had no equivalent. Fixed with the same floor.
5. **Trivial, fixed anyway:** `fossh-tui` had no `#![forbid(unsafe_code)]` despite containing zero `unsafe` — every other crate in this project has it; added for consistency. `data.rs`'s `start_of_today` used `%`, which overshoots the day boundary for a negative timestamp (`rem_euclid` doesn't) — unreachable today since its only caller's fallback is exactly `0`, fixed anyway since the fix is free and the function isn't `pub` so nothing enforces non-negativity at its own boundary.
6. **Acknowledged, deliberately not fixed:** a site slug containing wide/multi-byte Unicode characters will misalign the Telemetry screen's fixed-width columns (Rust's `{:<N}` pads by `chars().count()`, not display width). Confirmed this cannot panic — cosmetic only. Not worth a `unicode-width`-aware padding routine for what is, in practice, an ASCII-slug table; narrow enough not to be worth the complexity, same call as the SELinux reclaim-check nit in §3.2's retrospective.

**Verification:** 18 tests in `fossh-tui` now (up from 8) plus 1 new test in `fossh-admin` (`hash_of` round-trips against `generate()`'s own hash) — every fix above has a regression test, including a new `app.rs` test module that didn't exist before this pass (`esc_during_token_entry_cancels_back_to_dashboard_instead_of_quitting`, `nav_and_quit_keys_are_captured_as_token_text_during_input`, plus two nav sanity checks). `cargo clippy -- -D warnings` and `cargo fmt --check` clean. Full workspace re-run clean after all of it: root 291 tests (was 280), `fossh-ffi` 32 tests, 0 failures. Rendered every screen through `ratatui::backend::TestBackend` at two terminal sizes (100×30 and a narrow 60×20) via a temporary test, confirmed no clipping under the new padding/rounded-border treatment and that the context-sensitive footer actually shows the right hint per screen/step, then removed the temporary test — the same "write it, verify, revert cleanly" discipline the adversarial-review pass itself used for the pre-existing-file repro.

Also folded in, same pass, per a separate direct request ("tui has to feel modern but has a taste of retro, cozy, comfy, easy to use"): a shared `theme.rs` (warm amber/sage/terracotta accents layered over the terminal's own default fg/bg, never forcing a background — stays readable on light and dark terminals alike), rounded borders and consistent padding on every panel, and color-coded telemetry status (public/private/disabled). No copy/wording changes beyond what the bug fixes above required — `RULES.md`'s cold/plain/no-slop tone mandate predates this request and is a stronger, earlier-established constraint than a new aesthetic ask implies rewriting text, so the "cozy" treatment is delivered entirely through the visual layer, not through softened language.

### `fossh-admin::data_key` — a genuine concurrency bug, found while re-verifying after an M7 refactor (not part of M7 itself)

Re-running the full workspace suite after starting M7's shared-ingest-logic refactor below, `data_key::tests::concurrent_first_callers_all_converge_on_the_same_key` failed — then passed on retry, which is worse than a clean failure, not better: an intermittently-failing test that gets re-run into a green result is exactly how a real bug hides in plain sight. Isolated it: roughly 2 failures in 5 independent runs, always the same panic (`Corrupt`) from a losing thread's `fs::read` fallback.

Root cause and fix are recorded in full in ADR-0034 — summary: the previous `generate_and_write` had a real window between winning the `create_new` race and finishing `write_all`/`sync_all`, during which a concurrent loser could read a still-empty file and correctly-but-wrongly conclude it was corrupt. Fixed with the standard write-temp-then-atomically-publish pattern (`hard_link`, not `rename` — `rename` replaces rather than fails on an existing destination, which would have let two winners silently disagree on the key instead of one loudly failing). Stress-tested 40 independent runs post-fix, 0 failures, against a measured ~40% failure rate before.

Not a security bug — S2's fail-closed behavior was working exactly as designed, it was just triggering more often than it needed to, only in the narrow window right after a fresh install before `.data_key` exists at all. Recorded here rather than silently folded into the M7 commit since it's a real, independent fix to already-shipped §3.8 code, not new M7 work.

### M7 — FastCGI + optional HTTP listener (original spec, not a prrr.md §3.x sub-chapter)

Not part of the Fedora Native Deployment chapter this file otherwise tracks — M7 is the original `fossh-implementation-prompt.md`'s own milestone 7, picked up because it was the largest genuinely unstarted piece of the base product and this session had the room to do it properly, adversarial review included.

**Built:** a new crate, `crates/fossh-fcgi` — a persistent process speaking FastCGI directly over a Unix socket, for deployments wanting higher throughput than a fresh `fossh-cgi` process per request. Hand-rolled the wire protocol itself (`protocol.rs`: the 8-byte record header, `BEGIN_REQUEST`/`PARAMS`/`STDIN`/`STDOUT`/`END_REQUEST`, the length-prefixed name/value encoding PARAMS and CGI environment variables share) rather than pulling a `fastcgi` crate — same reasoning this project already applies to CRC-32/base32/HLL: small format, security-relevant (untrusted bytes off a socket), worth owning and reviewing directly. `connection.rs` assembles one full request per the canonical BEGIN→PARAMS→STDIN order every real FastCGI client (nginx included) actually sends, mapping PARAMS onto the exact same `fossh_ingest::ingest::IngestEnv` shape `fossh-cgi` already builds from real CGI env vars. `writer.rs` is a single dedicated thread owning the one `Store` handle this process ever touches, batching accepted events every 100 events or 500ms into one shared transaction per flush (`fossh_store::Store::record_events_batch`, new) instead of committing per-event.

**Refactored ahead of building it:** extracted the auth/rate-limit/pipeline-dispatch logic `fossh-cgi::handler` used to inline directly into `fossh_ingest::ingest::decide()`, so both transports share the actual security-critical path instead of `fossh-fcgi` duplicating it. `fossh-cgi`'s own behavior is unchanged (its test suite proves this — the auth/ratelimit/routing scenarios it used to test directly moved with the logic into `fossh_ingest::ingest`'s own tests, which didn't exist before). Also moved `forwarded.rs` (X-Forwarded-For trust) from `fossh-cgi` to `fossh_ingest`, since `fossh-fcgi` needed the identical logic.

**A real, independent concurrency bug found and fixed along the way, not part of M7 itself:** `fossh-admin::data_key`'s own concurrent-first-caller race — see the entry directly above this one.

**Adversarial review — a genuine, from-scratch protocol/concurrency review, not a rubber stamp.** Five real findings, all fixed and re-verified; two considered and deliberately left as documented, accepted tradeoffs rather than silently skipped. Full detail and reasoning: `DECISIONS.md` ADR-0036. Summary:

1. **High severity, fixed:** no read/write timeout anywhere on an accepted connection — a handful of connections that connect and send nothing permanently wedged the worker that picked each one up (the blocking reads in `connection::read_request` never return on their own), and with the default 8-worker pool, as few as 8 such connections exhausted it, permanently, for the rest of the daemon's life. Fixed with a 30-second timeout in both directions, set before a connection ever reaches a worker.
2. **Low severity, fixed:** the PARAMS accumulator checked its size bound *after* appending, unlike STDIN right below it, which already checked before — bounded either way by one record's own 65535-byte wire cap, but a real, worth-fixing inconsistency. Writing the regression test surfaced a second bug in the *test itself*: a filler sized at exactly `MAX_PARAMS_BYTES` silently wrapped to a wire record length of `0` when cast to the format's own `u16` field, turning an intended "one big record" into an accidental empty end-of-stream marker — caught because the test failed with the wrong error, fixed by capping the test's filler at the real per-record ceiling (`u16::MAX`) instead.
3. **Moderate severity, fixed:** a real, empirically-measured window where the socket file existed at `0755`, not `0600`, between `bind()` and a follow-up `chmod()` — this project already had the *correct* pattern for exactly this problem, in `fossh_store::Store::open` for `fossh.db`, and the socket-bind code just hadn't been written the same way. Fixed with a restrictive process umask held only across the `bind()` call. Verification for this one is deliberately *not* an automated `#[test]` — see the code comment at `manual_check_bind_with_correct_permissions_is_0600_and_restores_umask` in `crates/fossh-fcgi/src/main.rs` for why: writing it as a normal test first caused a real, reproducible intermittent failure in a *different*, unrelated test whenever `cargo test`'s default concurrent execution happened to overlap the two, because `umask` is real process-global state. Verified by hand instead, with a real, working, empirically-confirmed repro (run once with `--test-threads=1` against a temporarily-restored `#[test]` attribute, then reverted) — this project doesn't otherwise need a test-serialization dependency for one narrow case.
4. **Low severity, fixed:** stale-socket removal didn't check whether the socket was actually stale before removing it — a second accidentally-started instance would have silently hijacked a live one's socket, with no warning printed anywhere. Fixed: a real connect attempt first; only a connect failure is treated as safe to remove and rebind.
5. **Considered, left as a documented accepted risk:** both `mpsc` channels (connections, accepted events) are unbounded. Not fixed this pass — bounding them trades one failure mode for another that deserves its own deliberate design rather than being bolted on, and finding #1 above substantially defuses the worst compounding case the review named (wedged workers plus an unbounded events channel), since workers can no longer wedge forever.
6. **Considered, left as a documented, deliberate choice:** oversized PARAMS gets a raw connection close with no response, while oversized STDIN gets a proper 413 — a real asymmetry, but PARAMS content in real deployments is essentially always webserver-generated (nginx sets it, not the visitor), so hitting this path in practice means misconfiguration or an actual probe, not legitimate traffic. Judged not worth the added complexity.

Also confirmed correct by the same review, via real constructed repros rather than trusting the code as written: the `SAVEPOINT`-per-event rollback in `fossh_store::record_events_batch` genuinely undoes real partial writes under a mid-function failure (not just an already-failed no-op — the existing test only exercised the latter); `keep_conn`'s two-requests-on-one-connection lifecycle; and the worker pool's mutex being held only across `.recv()`, never across an actual request being handled.

**Verified against the actual compiled binary, not just `cargo test`:** a real `fossh site create` against a scratch data directory, a real `fossh-fcgi` process started with a real `fossh.toml` and bound to a real Unix socket path, a hand-rolled Python FastCGI client (deliberately independent of this crate's own protocol code) sending a real healthz request (204), a real unauthenticated ingest request (401), and a real authenticated one (204) — then reading the resulting `fossh.db` directly with Python's own `sqlite3` module and confirming exactly one row landed with the correct site, timestamp, and hashed visitor.

**Packaging:** `packaging/systemd/fossh-fcgi.service` (own hardening directives matching `fossh-fcgiwrap.service`'s, `PrivateNetwork=yes` included — this process's own network footprint, like `fossh-cgi`'s, is exactly zero); added to `packaging/rpm/fossh.spec` (binary, service unit, `%post`/`%preun`/`%postun` scriptlets alongside the existing CGI-mode ones — both transports ship enabled but not started, per the same "no enrolled key yet" reasoning already governing `fossh-fcgiwrap.socket`). Rebuilt the actual RPM after these changes (`rpmbuild -bb`, real, not `rpmspec --parse`) and confirmed via `rpm -qlp`/`rpm2cpio`+`file`+`strings`: `fossh-fcgi` is present, stripped, and free of leaked build paths, same as the other three binaries.

**Honest, open gap, not silently glossed over:** `fossh-fcgi` has no SELinux domain of its own. `packaging/selinux/fossh.te`'s existing `fossh_cgi_t` grants deliberately never covered the SQLite database at all (`fossh-cgi` "never opens the actual SQLite database itself" — see that file's own comment) since `fossh-cgi` genuinely doesn't touch it; `fossh-fcgi`'s entire design is the opposite; it opens `fossh.db` directly on every batch flush. With no `type_transition` targeting `/usr/bin/fossh-fcgi`, it runs in whatever domain execs it — typically `unconfined_service_t` on Fedora's targeted policy for a unit with no `SELinuxContext=` of its own — which is functional, not broken, but without the same kernel-enforced confinement `fossh-cgi` has. A real SELinux domain for direct-SQLite access (needing `lock` on the database file, unlike the spool-only path — SQLite's own concurrency control uses POSIX advisory locking) is real, separate follow-up work, not done this pass; noted in `packaging/systemd/fossh-fcgi.service`'s own header comment too, not just here.

**Not done, and explicitly out of scope this pass (ADR-0035):** a spool-mode alternative for `fossh-fcgi` (it only ever writes direct-to-SQLite, per §7.2's own framing of that as what FastCGI mode *is*, not a configurable choice); systemd socket activation (binds its own socket path directly instead); the optional loopback/Unix-socket-only HTTP listener (`feature = "http"`) M7 also names — `fossh_core::config::Config` already has the `[listener]`/`ListenerConfig`/S7-validation groundwork for it (built ahead of this session, confirmed still passing its own tests), but no listener code was written against it this pass.

Full workspace, after everything above: 329 tests in the root workspace (14+6+21+96+31+98+45+18, up from 280 before this session's TUI/M7 work), 32 in `fossh-ffi`'s separate workspace, 0 failures, `cargo clippy -- -D warnings` and `cargo fmt --check` clean in both.

### §3.3 — OCaml watchdog, first slice (logic layer: supervision, auth-gate primitives, tamper detection)

First OCaml code in this project. Built: `Nonce` (CSPRNG), `Auth` (challenge-response signature verification), `Session` (short-lived tokens), `Manifest` (clearsigned tamper-detection manifest), `Supervisor` (spawn/waitpid + restart-storm guard + tamper gate), `Gpg_status` (GnuPG `--status-file` protocol parsing), `Subprocess`/`Tempfile`/`Fileutil` (shared plumbing). Deliberately scoped to the logic layer only — no QUIC transport (§3.4), no Unix-socket bootstrap handoff (§2.4), nothing that accepts a network connection yet — per prrr.md §4's own instruction not to batch sub-chapters before checking any of them.

This is the one sub-chapter in this chapter that ran the full mandated QA gate (sandbox → implement → stop → adversarial subagent → fix → retrospective → this table) in the intended order, on the first pass, rather than being caught retroactively like §3.9 above. The adversarial review found 10 real, independently-reproduced findings — full detail in ADR-0041, not repeated here — two of them critical: the initial process spawn bypassed the tamper check entirely (only restarts went through it), and a validly-signed manifest that never mentioned the program actually being executed passed trivially, meaning the tamper-detection guarantee this whole component exists to provide wasn't actually bound to what got run, in two independent ways. Also found: `gpgv`'s bare exit code doesn't reflect key revocation or expiry at all (fixed by switching to full `gpg --verify`/`--status-file` and cross-checking the exact status-line semantics against GnuPG's own shipped `DETAILS` documentation, not assumed); a real, reproduced subprocess deadlock once manifest content passed roughly the OS pipe buffer size; four distinct uncaught-exception crash scenarios on the hot restart path. All 10 fixed and re-verified against the real compiled binary in the same pass — none deferred, except one (constant-time session-token comparison) that's genuinely unreachable until §3.4 wires up a network listener, which is documented as the reason, not silence.

Test suite grew from 31 to 41 checks across the fix pass, every one exercising real `gpg`/`gpgv`/`sha256sum` subprocesses and real spawned children — including a real revoked-key rejection test, which required first discovering (by directly inspecting the revocation certificate's own bytes) that GnuPG deliberately prefixes it with a colon specifically to block accidental scripted import, a real and documented safety feature, not a bug in this codebase. `dune build`/`dune test` clean, 3 consecutive runs, zero lingering `gpg-agent` processes confirmed each time.

### Comprehensive test/security/benchmark pass (scheduled, whole-project, before the final github-pages task)

Run deliberately as a distinct, dedicated pass across everything built so far — not folded into any single sub-chapter's own QA gate — specifically because a per-sub-chapter gate checks that *sub-chapter* in isolation, and a project this size can accumulate cross-cutting gaps (stale lockfiles, a `deny.toml` copied to a new crate without its allowlist keeping up, a benchmark nobody happened to run) that no single sub-chapter's own review would ever surface.

**Tests:** every workspace's test suite run clean — the root Rust workspace, `fossh-ffi`'s own separate workspace, `fossh-quiche-ffi`, `fossh-ipc`, and the OCaml `watchdog/` project — plus `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` across every Rust workspace, all clean. The `cargo-fuzz` targets from §3.7 re-run as a spot check (short runs, matching the original pass — the longer unattended run remains a real, separately-tracked follow-up, not silently claimed done here).

**Security/supply-chain findings — three real gaps, all fixed:**
1. `crates/fossh-quiche-ffi` and `crates/fossh-ipc` (both added this chapter) had no `deny.toml` at all, meaning `cargo deny check` never actually covered them despite the root workspace's own template implying every crate should have one. Added, copied from the established template.
2. That addition surfaced a real license gap once it actually ran: `libloading` (pulled in transitively via `bindgen`, which `boring-sys` needs for its own BoringSSL bindgen step) uses the ISC license, not yet on either new `deny.toml`'s allowlist. ISC is OSI-approved and permissive, consistent with every other allowlisted license in this project; added.
3. `fossh-ffi/Cargo.lock` was stale — it never picked up `nix`, added as a dependency of `fossh-admin` (which `fossh-ffi` depends on) in an earlier commit, meaning `fossh-ffi`'s own separate workspace was building against a lockfile that didn't actually describe its own dependency graph. Regenerated; re-verified 32/32 tests still passing in that workspace afterward.

Also found and fixed for consistency, not because it was exploitable on its own: `crates/fossh-quiche-ffi/src/lib.rs` was missing the `#![forbid(unsafe_code)]` attribute every other non-FFI crate in this project carries (the crate has no functional code at all — see its own doc comment — so this was a hygiene gap, not a live risk).

**Benchmark:** see §3.4's row above and ADR-0045 — a real, measured 24x/30x performance bug in `fossh-ipc`'s QUIC connection driving loop, found specifically because this pass required a benchmark rather than stopping once tests were green. This is the headline finding of this pass: correctness and performance are different questions, and the sub-chapter's own QA gate (ADR-0044) had already answered the first one honestly without ever touching the second.

**Residue cleanup:** all scratch benchmark programs and directories created during this pass deleted after their numbers were captured into permanent doc comments/ADRs; `git status` confirms nothing untracked or stray remains in the repository itself; the session's own scratchpad directory (outside the repository, never shipped) cleared as well.

No ADR filed for this retrospective entry itself — the one real substantive finding (the QUIC timeout) has its own ADR-0045; the license/lockfile/attribute gaps were small enough, and specific enough to *this* pass rather than a standalone architectural decision, to record here instead.

### §3.4 — OCaml `ctypes` bindings to `libquiche` (watchdog's half), closing out both sides of the QUIC transport layer

Ran the full mandated QA gate in the intended order: implement → stop → adversarial subagent review → fix → this retrospective, matching §3.3's own first-slice precedent rather than the retroactive catch-up §3.9 originally needed. Scope deliberately matched `fossh-ipc`'s own already-reviewed limit (ADR-0044/ADR-0045): real, verified connection establishment and basic stream send/recv, not the app-level command protocol or session-token/replay-protection binding §3.4 itself still asks for on either side — that remains explicitly open, not silently folded in.

Getting a hand-written `ctypes` FFI layer to a real C library actually working, rather than merely compiling, took most of this pass's real effort — three separate linker/symbol-visibility failure modes, each invisible until actually running the result, are detailed in ADR-0046 and are not repeated here. The throughline across all three: `ctypes`' `Foreign.foreign` resolves symbols the way `dlsym` does, at runtime, which the ordinary static/dynamic linker's own "is this actually referenced" reasoning has no visibility into — a lesson worth remembering the next time this project adds a `ctypes` binding to anything else.

The adversarial review found 9 real, independently-verified findings — full detail in ADR-0046, not repeated here — the most serious being a genuine, reproduced double-free (`Quic.close` was not idempotent; calling it twice crashed the process) and an RPM packaging ordering bug (`strip` referencing a file before the line that creates it, which would have aborted every single package build). Both fixed and re-verified; the double-free fix specifically got its own dedicated regression test, on the theory that a safety property serious enough to crash a process on a caller mistake deserves a test that pins it down directly rather than relying on some other test happening to exercise it incidentally.

One adversarial-review finding was deliberately *not* fixed asymmetrically: a peer resetting a single QUIC stream (`STREAM_STOPPED`/`STREAM_RESET`) is not always caught by `drive_until`'s own `is_closed()` check, since a single-stream reset does not close the whole connection — meaning `recv_from_stream` can fail slow (to the caller's deadline) rather than fail fast in that specific scenario. This exists identically, and pre-existingly, in `fossh-ipc`'s own already-adversarially-reviewed Rust implementation of the same design — fixing it only on the OCaml side would leave the two implementations of the same channel behaving differently for no good reason. Documented as a real, tracked follow-up for both sides together, not silently accepted and not fixed piecemeal.

RPM packaging (new `BuildRequires`, a `%build` vendoring step, `libquiche.so.0` shipped alongside `fossh-watchdog`) was closed out in the same pass rather than deferred, matching how §3.11's packaging gaps have been treated throughout this chapter — validated first with `rpmspec --parse` after the ordering fix, then with a real, full `rpmbuild -bb --nodeps` against the actual committed tree, producing a real, installable RPM and a clean `rpmlint` pass (one new, fully-traced, confirmed-harmless finding — see ADR-0046 and the table row above). Not left as a documentation-only claim this time.

### Chapter closing retrospective — "Fedora Native Deployment, Watchdog & Local Admin Hardening" (prrr.md)

Checked directly against prrr.md §5's own definition of done, bullet by bullet, rather than assumed satisfied because most of the work feels finished. **Updated 2026-08-06: both items this section originally listed as open are now closed — see the corrected bullets and net assessment below. The original text (§2.1 unenforced, no clean-VM run) is preserved in git history, not silently erased; this entry was found stale by this project's own final whole-repo sanity sweep, which correctly flagged that leaving it as written would contradict this file's own top entry.**

- **"All 11 sub-chapters (§3.1–§3.11) pass the QA gate individually."** All eleven now do. §3.3's own three listed responsibilities — restart-on-crash, tamper detection, and "enforce the auth gate (challenge-response, §2.1)" — are all real and live-verified as of this session: the auth-gate enforcement gap this bullet originally described (nothing enrolled an operator key, nothing called `Auth.verify_signature` outside its own tests) is closed by ADR-0057's setup-token-gated enrollment flow, confirmed by a full-repo grep that `Operator_key.enroll` has exactly one caller anywhere, and separately confirmed by a real, live, manual run of the full flow (setup-token submission, key enrollment, challenge-response, session-token issuance, all against the real compiled binaries, not just the automated test suite).
- **`cargo audit`, `cargo deny`, and `opam` dependency audit all clean.** Confirmed, including a fresh re-run today after task #31's QUIC wiring grew the main workspace's own dependency graph to 277 crates (§3.10's row above), and again after this session's release build started exercising that graph unconditionally (found and fixed a real `ISC`-license gate gap in the process) — still clean.
- **glibc floor check verified to hard-refuse on a below-floor host in a test environment, not just in theory.** Done, §3.1.
- **Fresh `dnf install fossh` on a clean Fedora VM verified end-to-end.** Done, 2026-08-06 — the first real clean-VM run this project has ever done (see this file's top entry for full detail). Found and fixed 3 real bugs invisible to `rpmbuild`-level verification alone (missing `Provides: user()/group()`, unexpanded SELinux `gen_context()` macros, an untracked-files gap in the release-tarball snapshot script). Real `dnf install` succeeded post-fix; real `%pre` user/group creation confirmed; the watchdog correctly refused to launch with no manifest present (fail-closed, live-confirmed); real AVC-clean service starts under `setenforce 1`; the full §2.6/§3.11 setup-token wizard flow ran end to end via a real interactive `fossh-tui` session over serial console, including confirmed reuse-prevention after burn — see this file's top entry for an important scope caveat found later the same day: that run predates the real watchdog-owned setup-token protocol and exercised the TUI wizard's own standalone/demo mode, not the real protocol (which a separate live test confirmed works, just not yet through the wizard screen itself).
- **`DURUM.md` reflects current state.** This file, kept current after every sub-chapter closes, including this entry — and including this entry's own correction, found by this session's final sanity sweep rather than left to a future reader to notice the contradiction.
- **A short closing retrospective summarizing deviations from §2's locked decisions.** This entry. No deviation from any of §2.1–§2.6's locked decisions occurred at any point — §2.1's auth-gate model was implemented exactly as originally specified (challenge-response, nonce-sign-verify, session tokens), just on a delayed timeline within release 1 rather than in an earlier pass.

**Net assessment:** the chapter is now fully done by its own stated bar — both items this retrospective originally listed as open (§2.1's auth-gate enforcement; a real clean-VM install verification) are closed, live-verified, not just claimed. See ADR-0052 for the original reasoning behind deferring them, and this file's top entry plus ADR-0057 for how and when each was actually closed.
